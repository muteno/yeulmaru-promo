node_modules
.wrangler
# src/index.js·wrangler.toml = Worker 소스, git 추적함(배포만 wrangler로 별도 — AGENTS.md 참조).
#   과거 "Worker 소스는 git 밖" 시절의 무시 규칙(src/·wrangler.toml)은 실체와 모순이라 제거(260801).

# Office documents (SharePoint sync 파일 - 절대 commit 금지: PIN/비번 평문 포함)
*.xlsm
*.xls
*.xlsx
*.docx
*.doc
*.pptx
*.ppt
# 예외: 티켓 등록 신청 기능의 의뢰서 템플릿 (앱 런타임 에셋 — 데이터 마스터 아님, PIN/비번 없음)
# 260710 xlsx→xlsm 교체: 운영자 개선판 계승 템플릿(분류 그룹·구분→장소 연동·첨부 VBA), 개인정보 스크럽·검증 후 커밋
!reference/ticketlink_template.xlsm

# MISO 이식 DB 반입 원본 (마스킹 전 CSV — 커밋 금지, 병합 산출물 이관본/miso_db.json·이관본/data/만 커밋)
data/db_export/*.csv
# 회원 DB MISO 반입 패키지 (PII — 절대 커밋 금지, MISO 직접 업로드 전용)
이관본/비공개/

# Backup / old files
*.old-*
*.bak
*~

# 이중 확장자 (실수 방지: claude.md.md 같은 거)
*.md.md

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Environment / credentials
.env
.env.*
*.key
*.pem
*.token
_ncp_secret.txt
_*_secret.txt

# 계정 폴오버 신호(러너 임시 — shared/claude_failover.js 가 활성 계정 쿼터 시 생성, 커밋 금지)
.nomute_active_quota

# Build output
dist/
build/
# 예외: 플레이그라운드 애니어그램 툴킷의 빌드 소스(스크립트·폰트 b64)는 재빌드 재현용으로 추적
!playground/enneagram/build/

# Logs
*.log

# 핸드오버 동봉 파일 (백업 폴더로 옮기는 게 원칙)
260526_yeulmaru_promo_*.js
260526_yeulmaru_promo_*.md

# E2E 하네스 임시파일(tools/scratch/e2e_*.py가 레포 루트에 생성 — try/finally 정리되나 강제종료 잔존 대비)
_e2e_*

# Claude Code 로컬 상태(워크트리 등) — 추적 안 함. 단 디자인 기틀 훅·설정만 추적 예외(260703)
.claude/*
!.claude/settings.json
!.claude/hooks/
__pycache__/

# 발행본 검증 미러(임시 — verify_published.mjs 산출)
.miso-mirror/

# 세션 실험용 index.html 사본(평의회 구역별 _wk_*) — 2MB 단일파일 복제라 절대 커밋 금지, 검증 끝나면 삭제
_wk_*.html
_bizzoom_before.html
