# 🎨 디자인 기틀 (SSOT) — yeulmaru-promo

> **이 문서가 이 repo UI의 단일 진실 원천(SSOT)이다.** UI를 만지는 모든 세션(Claude·타 모델·사람)은
> **여기 있는 토큰과 정본 컴포넌트만으로 구현**한다. 여기 없는 값/형태가 필요하면 **작업을 멈추고
> 운영자에게 질문** — 아래 '묻고-편입 프로토콜' 참조. 임의 창작 절대 금지.
>
> 기계 게이트: `tools/check_design.py` (편집 직후 `.claude/hooks/design_gate.py` + 커밋 시 `.githooks/pre-commit`).
> 마지막 실측: 2026-07-04 (raw hex index=1758 · signage=2 · :root 2블록/0블록 · 고아 12).
> **정본 위치 인덱스(규칙 2·절대 준수) = `docs/절대명령2_정본인덱스.md`** — 이 문서·확립본·게이트의 위치는 거기가 SSOT(새 정본은 등재해야 정본).

---

## 0. 브랜드 컬러 확립본 — 팔레트 폐쇄형 (운영자 지시로 정립, 260704)

> **⛔ 이 컬러칩이 전부다.** 아래 칩 **외의 색을 절대 들여오지 않는다** — 다른 팔레트에서 가져오거나,
> 기존 칩과 **비슷한 색을 새로 만들어 칠하는 것 금지**(유사색 난립 = 브랜드 붕괴). **이 폐쇄성이 브랜드의 핵심**이다.
> 각 역할색은 **딱 하나**. 새 색이 정말 필요하면 임의로 만들지 말고 **운영자 확인 → §3 3점 세트**로만 편입.
> 값 SSOT = `index.html :root` 2블록(§1) — 이 절은 그 **역할 확립본(요약)**. 시각본: `docs/브랜드컬러_확립본.html`.

### ① 브랜드 코어 — 인디고 (유일 코어)

| 토큰 | 값 | 역할 |
|---|---|---|
| `--accent` (=`--c1`) | `#4A4DE7` | **인디고 = 예울마루 아이덴티티(불변).** 주 버튼·링크·선택·포커스·프레임. 틴트 사다리 = `--accent-light #E8E8FD` · `--accent-glow`(15% 글로우) |

### ② 서브코어 — 살몬 (유일 웜 서브)

| 토큰 | 값 | 역할 |
|---|---|---|
| `--peach-text` (=`--c2`) | `#D88455` | **살몬 = 보조 브랜드(웜).** 현수기·입간판 카테고리, 여수 도시색. 틴트 사다리 = `--peach #F0C4B8` → `--peach-light #FBF0EC` → `--peach-bg #FDF6F3`(페이지 배경 축) |

### ③ 카테고리 축 — `--c1~--c6` (임의 시리즈 전용 팔레트)

**의미 없는 임의 다중 시리즈**(공연 순번 등)와 매체 구분(홍보 지도)은 이 6색 고정 순서(`_CPAL` ~L4682). **신규 임의 시리즈 팔레트 창작 금지.**
⚠️ 단 **의미 있는 색축은 별개** — 장르 `_BIZ_GENRE_COLORS`·기획/대관 `_BIZ_TYPE_COLORS`(~L4677, 팔레트 밖 raw 포함)는 c축을 **쓰지 않는 게 정본**(코드 주석 "의미 있는 색에는 절대 쓰지 않음")이며 동결 부채(§6-6)로 그대로 유지. 신규 의미색 축이 필요하면 운영자 질문.
비교 시리즈가 주색(c1=`--accent`)과 겹치는 차트는 **c2부터 시작**(기존 pal 관행, ~L3911) — c1부터로 "교정"하지 말 것.

| 토큰 | 값 | 동값 별칭 | 역할 |
|---|---|---|---|
| `--c1` | `#4A4DE7` | =`--accent` | 인디고 — 예울마루 (⚠️ 토큰 직접 참조는 고아·§1 — 실코드는 `--accent`로 참조, 시리즈 pal도 c2부터) |
| `--c2` | `#D88455` | =`--peach-text` | 살몬 — 현수기·입간판 |
| `--c3` | `#1A6B3C` | =`--green` | 딥그린 — 순천 도시색(성공/완료와 동일 칩) |
| `--c4` | `#C02872` | =`--insta`(고아) | 마젠타 — 전광판, 광양 도시색 |
| `--c5` | `#2D8AB3` | — | 시안블루 — 협력기관 DID |
| `--c6` | `#F5B400` | =`--kakao`(블록2, 승) | 옐로 — 카카오 |

> **동값 별칭 = 같은 hex를 역할별 이름으로 부르는 것 → 정상 공존**(§3.5②). "중복이네" 하고 억지 병합·삭제 금지.
> 단 이는 **기존 별칭의 공존 판정 기준**일 뿐 — **신규 동값 별칭 토큰 신설도 §3 승인 필수**(임의 신설 금지).

### ④ 역할색 단일 지정 (각 딱 하나)

| 역할 | 유일 칩 | 비고 |
|---|---|---|
| 성공/완료 초록 | `--green #1A6B3C` **(대표)** | `--blog`도 260705 이 값으로 별칭 통합. raw `#0F8B4C`(32회)는 동결 부채(§6-3) — **신규 사용 금지** |
| 카카오 옐로 | `--c6 #F5B400` **(대표)** | 블록1 `--kakao`는 260705 `var(--c6)` 별칭화(값 일치, 이중정의 완화) |
| 위험 레드 (유일·통일) | `--danger-btn #E24B4A` **(대표)** | **✅ 260705 빨강 1종 통일(운영자 확정)** — `--danger`는 `var(--danger-btn)` 별칭. 시그널(에러 텍스트·보더·아이콘·마감 임박·차트 하락)·버튼 채움 **공용**. hover 다크변형 `#C73F3E`(2곳)·차트 리터럴 8곳은 동결 |
| 채널색 blog·etc | `--blog=var(--green)` · `--etc=var(--c5 #2D8AB3)` | 260705 별칭 통합 |
| 채널색 insta·youtube | `:root` 고아 토큰 2종 | 사용 0회 동결(§6-4) — 신규 사용 전 운영자 확인 |

### ⑤ 중립 — 텍스트/서피스 사다리 (새 회색 창작 금지, 이 사다리에서만)

- **텍스트**: `--text #1A1A2E`(본문) → `--neutral-text #6B6B7B` → `--dim #888` → `--muted #bbb` (`--past-text`는 260705 `var(--muted)`로 통합)
- **서피스**: 흰색 글래스 축(`--surface-solid` · `--glass` 대표, `--surface`/`--glass-border`/`--glass-bd`는 260705 별칭) + 뉴트럴 `--neutral #EEEDF3`/`--neutral-d #E1DFEC` + 저채도 상태 배경(`--past-bg` 대표 · `--nm-bg`=별칭 · `--today-bg` · `--off-bg`=`var(--border2)` 별칭)
- **배경**: `--bg` 그라데이션(피치→라벤더→블루) 하나

### ⚠️ 부수 — 아직 정할 것 (임의로 안 고치고 짚음 = §6 동결 부채와 동일 항목)

- ~~유일 레드 미확립~~ → **✅ 260704 확립 → 260705 통일**(④ 참조): raw 76곳 → `var()` 62곳 치환. **260705: `--danger`를 `var(--danger-btn)` 별칭화 = 빨강 1종 `#E24B4A`로 통일**(대표 = `--danger-btn`). 잔존 리터럴 = 대표 정의 1 + 차트 내부 8곳(SVG ~L3993·Plotly ~L4333+, `var()` 불가 자리·값 동일) + hover `#C73F3E` 2곳.
- **c축 밖 차트색 = §6-6 동결**: 의미색 맵 2종(`_BIZ_GENRE_COLORS`·`_BIZ_TYPE_COLORS`) + 임의 시리즈 pal 끝자락 raw `#7C3AED`(~L3911 등 3곳) — 청산/토큰 편입은 운영자 판단.
- **그레이 축 raw 산개**(§6-5) — `--dim`/`--muted` 수렴 여부 대공사 대기.

---

## 1. 색·수치 토큰 SSOT — `:root` 2블록 (index.html)

새 색·반경·z-index가 필요하면 **여기(토큰)에만 추가**하고 `var()`로 쓴다. raw hex 직접 사용 금지
(기존 1,827개는 동결된 부채 — 늘리면 게이트가 차단).

### 블록 1 — 기본 팔레트·서피스·반경 (index.html L22, 34토큰 — 260704 `--danger`·`--danger-btn` 편입)

| 토큰 | 값 | 용도 |
|---|---|---|
| `--accent` | `#4A4DE7` | 주 강조색(브랜드 보라·버튼·링크) |
| `--accent-light` | `#E8E8FD` | 강조 연한 배경 |
| `--accent-glow` | `rgba(74,77,231,0.15)` | 강조 글로우/포커스 링 |
| `--peach` | `#F0C4B8` | 보조 피치 |
| `--peach-light` | `#FBF0EC` | 피치 연한 배경 |
| `--peach-bg` | `#FDF6F3` | 피치 서피스 |
| `--peach-text` | `#D88455` | 피치 텍스트 |
| `--bg` | `linear-gradient(135deg,#FDF6F3 0%,#F0EBF5 50%,#EBF0F8 100%)` | 페이지 배경 |
| `--surface` | `var(--glass-surface)` | 260705 별칭 통합(→`--glass-surface`) |
| `--surface-solid` | `#fff` | 불투명 서피스 |
| `--glass` | `rgba(255,255,255,0.55)` | 글래스 서피스 **(대표)** |
| `--glass-border` | `var(--glass)` | 260705 별칭 통합(→`--glass`) |
| `--glass-shadow` | `0 8px 32px rgba(74,77,231,0.08),0 2px 8px rgba(0,0,0,0.04)` | 글래스 그림자 |
| `--border` | `rgba(0,0,0,0.09)` | 기본 테두리 |
| `--border2` | `rgba(0,0,0,0.12)` | (고아) 진한 테두리 |
| `--text` | `#1A1A2E` | 본문 텍스트 |
| `--dim` | `#888` | 흐린 텍스트 |
| `--muted` | `#bbb` | 더 흐린 텍스트 (260704 청산 — 홍보 지도 피드 빈상태에서 사용) |
| `--past-bg` | `rgba(0,0,0,0.02)` | 지난 날짜 배경 |
| `--past-text` | `var(--muted)` | 260705 별칭 통합(→`--muted #bbb`) |
| `--nm-bg` | `var(--past-bg)` | 260705 별칭 통합(→`--past-bg`) |
| `--today-bg` | `rgba(230,240,255,0.8)` | 오늘 셀 배경 |
| `--off-bg` | `var(--border2)` | 260705 별칭 통합(→`--border2`) |
| `--green` | `#1A6B3C` | 성공/완료 초록 **(대표)** |
| `--danger` | `var(--danger-btn)` | 260705 별칭 통합(→`--danger-btn`) — 빨강 1종 통일 |
| `--danger-btn` | `#E24B4A` | **위험 레드 대표(빨강 유일)** — 시그널·버튼 공용 (260705 통합) |
| `--kakao` | `var(--c6)` | 260705 별칭 통합(→`--c6 #F5B400`) — 이중정의 값 일치 |
| `--insta` | `#C02872` | (고아) 인스타 브랜드 |
| `--youtube` | `#B71C1C` | (고아) 유튜브 브랜드 |
| `--blog` | `var(--green)` | 260705 별칭 통합(→`--green #1A6B3C`) |
| `--etc` | `var(--c5)` | 260705 별칭 통합(→`--c5 #2D8AB3`) |
| `--radius` | `16px` | 기본 라운드 |
| `--radius-lg` | `20px` | 큰 라운드 |
| `--radius-xl` | `24px` | 특대 라운드 |

### 블록 2 — 뉴트럴·레이어(z)·카테고리 컬러·셀 (index.html L1322, 26토큰)

| 토큰 | 값 | 용도 |
|---|---|---|
| `--neutral` | `#EEEDF3` | 뉴트럴 버튼/칩 배경 |
| `--neutral-d` | `#E1DFEC` | 뉴트럴 호버/눌림 |
| `--neutral-text` | `#6B6B7B` | 뉴트럴 텍스트 |
| `--r-btn` | `12px` | 버튼 라운드 (**260806 운영자 「★ 부드럽게」 승인 — 10px→12px**) |
| `--r-pop` | `16px` | 팝오버 라운드 |
| `--r-modal` | `20px` | 모달 라운드 |
| `--elev` | `0 20px 56px rgba(74,77,231,.16), 0 6px 18px rgba(26,26,46,.07)` | 부양 그림자 |
| `--backdrop` | `rgba(26,26,46,.42)` | 모달 백드롭 |
| `--glass-surface` | `rgba(255,255,255,.78)` | 글래스 서피스(진) |
| `--glass-menu` | `rgba(255,255,255,.45)` | 호버 세부메뉴 드롭다운 반투명 유리(운영자 승인 260709 · blur 9px 동반) |
| `--glass-bd` | `rgba(255,255,255,.6)` | 글래스 테두리 |
| `--z-sticky` | `10` | (고아) 레이어 축 — raw z-index 청산과 묶어 처리 대기 |
| `--z-nav` | `90` | (고아) 〃 |
| `--z-dropdown` | `1000` | 드롭다운 레이어 |
| `--z-modal` | `2000` | 모달 레이어 |
| `--z-confirm` | `2100` | (고아) 확인창 레이어 |
| `--z-toast` | `3000` | (고아) 토스트 레이어 |
| `--c1`~`--c6` | `#4A4DE7 #D88455 #1A6B3C #C02872 #2D8AB3 #F5B400` | 카테고리 팔레트(c1은 고아) |
| `--kakao` | `#F5B400` | ⚠️ 이중 정의(이 값이 CSS에서 승) |
| `--cell-pad-y` / `--cell-pad-x` | `10px` / `12px` | 캘린더 셀 패딩 |
| `--cell-fs` | `13px` | 캘린더 셀 폰트 |

> **signage/index.html은 :root 없음 + raw hex 2개(`#000`·`#333`)가 전부** — 그 상태로 동결.

---

## 2. 정본 컴포넌트 인벤토리 — 24종 (이 형태만 사용 · 헤딩 수는 표 증보를 못 따라와 「11종」→「16종」으로 두 번 낡았다 — **표가 정본**, 260810-2 실측 행수 24로 재동기)

새 화면·요소는 **아래 것들의 조합**으로 만든다. 새 CSS 클래스 남발 금지 (CLAUDE.md UI/UX 계승 원칙과 동일).

| # | 컴포넌트 | 클래스 | 비고 |
|---|---|---|---|
| 1 | **버튼** | `.btn` + 색 4변형 `--primary` `--neutral` `--ghost` `--danger` + 크기 3변형 `--sm` `--md` `--lg` (8변형) | 모든 신규 버튼은 이 조합. 인라인 스타일 버튼 금지. **260806 「덜 투박하게」 개정(운영자 「★ 부드럽게」 선택 · 시안 `docs/reports/260806_버튼_덜투박_플레이그라운드.html`)** = ① 눌림 `translateY(0) scale(.98)` → **`translateY(1px) scale(1)`**(누르면 판 **안으로** 들어간다 — 구판은 작아지기만 했다) ② 주 버튼 그림자 1겹 → **2겹**(`0 1px 2px α.2` 붙는 것 + `0 6px 16px α.16` 퍼지는 것 · 강조색 alpha 변주 §3.5②) ③ **`:focus-visible` 신설** — 구판엔 **키보드 포커스 표시가 아예 없었다**(접근성 결손). 링 = `0 0 0 4px var(--accent-glow)` = `.modal input:focus` 자구 계승 ④ 전이 커브 = 기틀 §2 #13 주력 `cubic-bezier(.4,0,.2,1)` ⑤ 라운드 `--r-btn` 10→12 · `--sm` 8→10 · `--md` 패딩 10/20→11/22. **`--btn-sh`** = 변형별 자기 그림자를 담는 **컴포넌트 로컬 변수**(`:root` 토큰 아님 · `.modal --mpad-*` 선례) — 포커스 링이 깊이를 지우지 않고 겹쳐 얹히게 하는 장치 |
| 2 | **유틸 서피스** | `.u-backdrop` `.u-pop` `.u-surface` `.u-empty` | 백드롭/팝오버/서피스/빈상태 공용 |
| 3 | **스위치** | `.sw` (+노브 `.sw-k`) · 글자형 변형 `.ry-live-tg` | ON/OFF 토글 전부 이걸 재사용 (예: 프로그램 폼 홍보 토글). **글자형 변형(운영자 지시 260804 「글자 자체가 on off 토글일 수 있게」)** = pill 없이 라벨 글자가 곧 스위치 — 켬 `--accent`/800 · 끔 `--dim`/600(색+굵기 이중단서 §3.5③) · `role="switch"`+`aria-checked` 유지. 도구 묶음처럼 **자리가 좁고 라벨이 이미 상태를 말하는 곳**에만 쓴다(현재 1곳: 판매 실적 「판매중」). 넓은 폼·설정줄은 `.sw` 그대로 |
| 4 | **아이콘 버튼** | `.icon-btn` | 헤더/모달의 정사각 아이콘 버튼 |
| 5 | **토스트** | `.toast` | 하단 알림. 긴 문구 nowrap 금지(모바일 뷰포트 확장 버그 전례) |
| 6 | **칩** | `.chip` `.chips` `.chip-dot` `.chip-count` `.chip-label` | 필터/토글 칩. **선택 상태에도 호버가 있어야 한다(260805-26)** — `.X.on`/`.active`/`.selected` 규칙이 `.X:hover`보다 뒤에 와서 호버가 바꾸던 색·테두리를 덮으면 **선택된 칩만 반응 0**이 된다(호버 감사 실측 = 8종이 그 상태였다). 색을 새로 만들지 말고 이 레포의 「채워진 버튼」 호버 정본 `.m-btn:hover{transform:translateY(-1px)}`를 그대로 얹는다 — 현재 적용 = `.nb-chip.on`·`.ana-chip.on`·`.prog-tab.active`·`.sp-chip.selected`·`.sp-time-unset.active`·`.fbar-seg button.on`·`.did-filter-btn.on`(+`.cm-tool`은 자기 transition에 transform을 선언해 놓고 `:hover`가 없던 경우) |
| 7 | **모달** | `.modal` `.modal-bg` `.modal-acts` `.modal-x` **`.mhead`(+`.mhead .sub`)** | 중앙 모달 골격. z는 `--z-modal` 축. **머리줄 = 부품까지 정본(260805-23 · 운영자 「모든 모달 창에 윗부분을 저거로 스모킹 패리티 고정 시켜서 항상 저 형식이 나오게끔해줘」)**: 앱의 **`.modal` 61곳 전부**가 강조색 밴드 머리줄 한 벌을 쓴다 — 모양 SSOT = CSS `.mhead`(`padding:18px 74px 18px 24px` · `background:var(--accent)` · 제목 16px/700 `var(--surface-solid)` 6.0:1 AA · 부제 `.sub` 11.5px/600 `rgba(255,255,255,.85)` 기존 알파스텝) · **내용 SSOT = 빌더 `_mhead(title, sub, extra)`**(`pr`(오른쪽 예약폭) 인자는 260805-30에 폐지 — 예약은 CSS `.mhead` 74px 하나뿐). 값의 출처 = 260805-22 「카카오 메세지 설계」·「AI 홍보 · 점검」 인라인 밴드를 **한 글자도 안 바꾸고** 옮긴 것(신규 색·수치 0). **밴드를 인라인으로 다시 타이핑하지 마라** — 260805-16 X버튼 사고와 같은 유입 경로(「CSS만 정본이고 내용은 문자열 복붙」)라 `tools/check_modal_head.py`가 **하드 0**으로 막는다. 패딩 상쇄 계약 = `.modal`은 `padding:var(--mpad-y,28px) var(--mpad-x,28px)`, 셸은 인라인 `padding:` 대신 `--mpad-*`(밴드가 그만큼 음수 마진으로 되밀어 폭 끝까지 칠해진다). 제목·부제의 **값**은 게이트가 안 정한다(자리마다 뜻이 다르다 · X 접근명과 같은 판단) · 이모지는 안 쓴다(운영자 260805-22 「이모지 가능한 안 씀」). **머리줄 위 X·최소화 버튼의 배경 = 흰 알파 축(260805-25 · 운영자 시안 C 선택)**: 쉼 `rgba(255,255,255,.12)` → 호버 `rgba(255,255,255,.25)` — 정본 `.modal-x`의 accent 틴트(`.08`→`.18`)는 **흰 표면을 전제로** 만든 값이라 같은 accent로 칠한 밴드 위에서는 두 상태가 전부 묻힌다(260805-23 직후 실측 = 호버 ΔE 0.7 · 변한 픽셀 **0.0%** = 무반응). 쓴 알파 둘 다 **이미 있는 스텝**(흰 알파 사다리 33단) = 신규 값 0 · §3.5② 알파 변주. 잉크는 쉼·호버 모두 `var(--surface-solid)` 유지. ⚠ 이 값은 **머리줄 위 한정** — 흰 표면 위 `.modal-x`(연간 일정 `↓`·`↗`의 인라인 흰 알약 등)는 종전 그대로. **머리줄 위 버튼 = 두 부품뿐(260805-27 평의회 5인 · 운영자 「무슨 의미의 픽토그램인지 모르겠고 · 디자인 최악 실 기어가는 것 같음」)**: ① **아이콘 원형 = `✕`(닫기)·`─`(최소화) 둘만** — NN/g가 인정하는 범용 관용구 축 · 흰 알파 알약 + 흰 글리프. ② **그 외 조작은 전부 텍스트 알약 `_mheadBtn`** — 뜻을 글자로 적는다. ⚠ **화살표·기호 글리프를 조작 버튼으로 쓰지 마라** — 실측 = `↓`는 잉크 5px·가장 얇은 획 **0.50px**(`✕` 21px의 1/4)이고 `font-weight:900`을 먹여도 **5px 그대로**(굵기 축 없는 폴백 폰트에서 나와 CSS로 굵게 만들 수 없다). 기틀 §2 #7이 이 둘을 「글자 버튼」으로 분류하고 L167 SVG 각주의 면제 대상은 `.icon-btn`·`.iobtn`뿐이라 **SVG 교체도 기각** — 답은 글자다. 외부 실증 = 라벨 있으면 예측정확도 88%/없으면 60% · GOV.UK는 「새 창」 아이콘을 폐기하고 글자로 갔다. 머리줄의 **조작 버튼**(`_mheadBtn` = 엑셀 복사·분석·일일입력·홍보 지도)은 흰 알약을 유지하되 호버를 신설 — `onmouseover`/`onmouseout`로 `var(--accent-light)`(앱 대표 호버 관용구 · `backToAdminMenu` 뒤로 버튼과 같은 문법). 실측 패리티는 `tools/smoke_modal_head.mjs`가 헤드리스로 확인. **첫 줄 높이 = 32px 한 갈래(260805-31 · 운영자 「좌측 타이틀, 버튼 +x 간에 모두 광학 잉크 단위로 중심부가 수평선에 있는지 검증」)**: `.mhead>span:first-child{line-height:32px}` — 머리줄 첫 줄의 높이는 그 줄의 가장 키 큰 요소가 정하는데, 조작 버튼이 있는 모달은 32(버튼 지름)·제목만 있는 모달은 21(글자)이라 **같은 머리줄이 밴드 높이 68 / 57 두 갈래**였고, 그래서 절대배치 `✕`를 어떤 고정 `top`에 걸어도 한쪽이 반드시 어긋났다(실측 −4.2 ~ +5.5px · `top:50%`는 기준이 밴드가 아니라 **모달 전체**라 키 큰 보드에서 X가 화면 한복판까지 내려갔다). 제목 줄높이를 버튼 지름 32에 맞추면 첫 줄이 언제나 32 = 밴드 높이도 **68 한 갈래**로 모이고 `.modal:has(>.mhead)>.modal-x{top:18px}`(= `.mhead`의 padding-top)이 제목·조작 버튼과 **정확히 같은 칸**이 된다(신규 값 0 — 32는 `.modal-x` 지름 계승). 밴드 안 `.bp-x`도 같은 32로 흡수. ⚠ 그 위에 **260805-31 광학 잉크 보정**(`.modal:has(>.mhead .modal-x)>.modal-x{top:19.33px}`)이 겹친다 — 밴드에 `─`가 서는 자리에서 `✕` 잉크중심(상자중심 −0.85)과 `─` 잉크중심(+0.48)의 **1.33px** 차를 상자로 상쇄해 **잉크끼리** 한 줄에 세운 것이다(운영자가 지정한 단위 = 상자가 아니라 잉크). 즉 **칸 계약(32 한 갈래)이 먼저 서고, 그 위에서 글리프별 잉크 보정이 성립한다** — 둘은 대체 관계가 아니다. 게이트 ⑥의 허용 폭 2px는 이 보정을 담기 위한 것. 실측 잔차는 **잉크 무게중심 ±2px 이내**(제목 글자 구성이 만드는 차 · 부제는 활자 크기가 11.5라 최대 4.2) — 칸(박스)은 Δ0. **머리줄이 있는 모달은 테두리 0 · 음영만(260805-32 → 35 · 운영자 「상단에 흰색 테두리가 살짝 보이게 하지 말고 상단부는 모두 코발트로」 → 「상단 양옆에 흰색으로 좀 실선처럼 보이는 것도 정상적으로 **음영만** 보이게」)**: `.modal:has(>.mhead){border:0!important}` — `.modal`의 1px `var(--glass-border)`(흰 알파 .55)는 흰 표면 위에선 유리 가장자리지만 **강조색 밴드가 덮은 윗변**에서는 밴드 위를 지나는 흰 실선으로 보인다(밴드는 padding 상쇄라 테두리 바깥까지 못 덮는다 — margin은 border를 가릴 수 없고 `overflow`도 패딩박스에서 잘린다). 1차 조치(윗변 색만 `var(--accent)`)로는 **좌·우 변이 밴드 높이만큼 코발트 옆 흰 실선**으로 남았다 — 테두리는 길이의 일부만 색을 달리할 수 없고(`border-image`는 모서리 반경을 못 따른다) 밴드가 덮을 수도 없다. → **테두리를 없애고 이미 있는 `box-shadow`(음영)에 가장자리를 맡긴다**. `box-sizing:border-box`라 겉 크기 무변 · 신규 값 0 · 「윗변만 다른 색」 예외도 같이 소멸. `!important`는 `.annual-modal`이 `border:… !important`로 다시 선언해 둔 한 곳 때문(규칙을 두 군데로 쪼개지 않으려는 것). 전 앱 실측 스캔 결과 같은 축(채운 블록이 둥근 테두리 상자 변에 밀착) = 머리줄 외 **카드 제작기 미리보기(`#cm-stage ← #cm-prevbox`) 1곳뿐**이고 그쪽 테두리는 `rgba(0,0,0,.09)` = **어두운 쪽**이라 밝은 실선으로 튀지 않는다(무변경). 게이트 = `tools/smoke_modal_head.mjs` 계약 ⑥·⑦(킬테스트 4/4 · ⑦은 「테두리 0」 또는 「밴드와 같은 색」 둘 중 하나면 통과) — ⑥은 기준값을 파일에 안 적고 밴드 자신의 `padding-top`·제목 상자에서 **그 자리에서 유도**해 비교한다. **X 아이콘 버튼 = 부품까지 정본(260805-16 명문화 · 2차에서 클래스 무관으로 확대)**: 앱의 **X 한 글자 버튼 83곳 전부**(`.modal-x`·`.bp-x`·`.panel-close`·`.sub-pop-close`·`.pm-co-x`·`.cb-close`·`todo-x`·인라인 등 **클래스 무관**) = 글자 `✕` **U+2715 고정** + `title`·`aria-label` **한 벌**. `×`(U+00D7 곱셈기호) 금지 — 같은 13px·같은 원인데 잉크 폭이 7.59 vs 13px = 41% 얇아 「힘없이 작은 X」로 보인다. **접근명 값은 자리의 뜻대로**(닫기 / 삭제 / 제거 / 비교 제거 / 영역 삭제 / 해제) — 게이트는 **있는지만** 묻고 값은 안 정한다. **게이트 ⑦이 하드 0으로 강제**. ⚠ 라벨이 X가 아닌 버튼(↓ 저장·↗ 새 창·‹ 목록으로·「취소」 등 글자 버튼)은 대상 밖. ⚠ **실측 하네스·시안에서 정본 부품을 손으로 다시 타이핑하지 마라** — 이 위반의 유입 경로가 정확히 그것이었다(`probe_modalx_canon.mjs`가 대조군을 `&times;`로 재타이핑 → 실코드 유출). 대조군은 실코드에서 뽑아 쓴다 |
| 8 | **폼/네비 프리미티브** | `.nb-btn` `.nb-chip` `.nb-input` `.nb-field` `.nb-label` `.nb-col` `.nb-flow` 등 `nb-*` | 위저드·폼 전반의 표준 입력/칩 |
| 9 | **캘린더 셀** | `.cell` `.cell-corner` | 셀 패딩·폰트는 `--cell-*` 토큰 |
| 10 | **하부 팝업** | `.sub-pop` (`-hdr` `-body` `-close` `-copy` `-title` `-bg`) | 조회 모달 위 2차 팝업 |
| 11 | **FAB·챗봇** | `.cb-fab` `.cb-hdr` `.cb-body` `.cb-chip` 등 `cb-*` · `.qa-fab`(+`-label`) · **자라는 대화창 `.cb-rise`(+자리 `.cb-rise-col`)** | 우하단 플로팅 위젯. **자라는 대화창(260813 · 운영자 「예울이에 마우스 포인터가 올라가면 대화창이 위에 거주지 top5 있는 구간까지 쭉 올라가줄래? 자연스럽게 이징해서 · 대화를 통해 고객질의할 수 있도록」)** = 칸에 박힌 채팅을 **호버·포커스에 열 전체로 펼치는** 한 벌. 현재 1곳(고객 분석 4면·모달의 예울이). **높이를 애니메이션하지 않는다** — 카드를 처음부터 열 전체 높이로 깔고(`position:absolute;inset:0`) 평소엔 아래 `--rise-h`만 `clip-path:inset(max(0px,calc(100% - var(--rise-h))) 0 0 0 round var(--radius))`로 보여준 뒤, 펼침 = **그 잘라내기가 0으로 가는 것**뿐이다(리플로 0·합성만). ⚠ 높이·마진 전이로 만들면 게이트 ⑥ 잰크 전이가 15 → 16이 돼 **커밋이 막힌다** — 이 형태가 그 계약을 피해 가는 정본 답이다. 문법 계승 = 모바일 네비 패널 `clip-path` 말림 · 커브 = 주력 감속 `cubic-bezier(.215,.61,.355,1)`(`.dd-menu`) · **590ms**(초판 460 → **260813-2 운영자 선택값** 회신 · 시안 [선택값 복사] 그대로 · 여정이 열 한 칸이라 §2 #24 리빌 420보다 길다) · 지연 = 열림 .1s(지나가는 마우스에 안 열림)·닫힘 .18s(스치듯 벗어나도 안 닫힘) · 신규 색·토큰 0. **자리 확보 = 호스트 열의 `padding-bottom:calc(var(--rise-h) + var(--rise-gap))`**(카드가 절대배치라 margin이 안 먹는다) → 위 카드 높이는 **적용 전과 Δ0**(실측 1500×1000: 열 651 · 막대 414 · 접힘 칸 223). `--rise-h` = 로그창 `--rise-log`(`clamp(96px,16vh,190px)` — **이 기능 배선 전 로그창 높이 그대로**. ⚠ **키우지 마라** — 접힘 칸은 호스트 열의 `padding-bottom`으로 **자리를 예약**하는 값이라, 키운 만큼 위 막대 카드가 실제로 줄어든다: 260813-2에서 운영자 선택값 「접힘 칸 304px」을 담으려 상한을 241로 올렸다가 **1500×1000 막대 414 → 333(−81)** · **1920×1080 474.8 → 406.6(−68)**로 눌렸고, 260813-3에서 낮은 창만 되살리는 상한(`--rise-avail`·`_memRiseFit`·`_MEM_BAR_MIN`·`ResizeObserver`)을 얹었다가 **260813-4 운영자 「위에 이미 있는 내용의 간격들은 그대로 유지하면서, 예울이 대화창이 켜졌을 때 잠시 슬라이드로 덮히는 정도만」**으로 **둘 다 되돌렸다**(그 상한도 함께 삭제 — 지금은 하한 159px에 먼저 걸려 어느 창에서도 발동하지 않는 죽은 코드였다). 넓어지는 건 **켜져 있는 동안 덮는 것**뿐 — 카드가 절대배치·불투명(`--surface-solid`)이라 위 막대 카드를 **밀지 않고 덮는다**. 실측 = 막대 카드 1500×1000 **414.0** · 1920×1080 **474.8** · 1366×768 **237.7** = **세 창 전부 배선 전과 Δ0**(접힘 칸 223 / 235.8 / 185.9). ⚠ **`--rise-h`에 백분율 금지** — 같은 변수를 `padding-bottom`(%가 **폭** 기준)과 `clip-path`(%가 **높이** 기준)가 함께 써서 한 값이 두 뜻이 된다) + 입력줄·테두리 `--rise-foot`(**`.cb-foot` 실측으로 JS가 덮어씀** — CSS의 63px은 폴백 · 활자·줌이 바뀌면 실측이 이긴다) = `.modal --mpad-*`·`.btn --btn-sh`와 같은 **컴포넌트 로컬 변수**(`:root` 토큰 아님). ⚠ **대화는 아래에 붙인다**(`.cb-rise>.cb-body>:first-child{margin-top:auto}`) — 로그창이 늘 열 전체 높이라 위로 붙이면 접힘 칸이 빈 칸이 된다. ⚠ 진입로는 **`:hover`와 `:focus-within` 두 개**(터치·키보드는 입력칸에 들어갈 때 열린다) · `prefers-reduced-motion`이면 전이 없이 즉시. 시안·실측 = `docs/reports/260813_예울이_호버확장_전후.html`(+ 캡처 4장 · 생성기 `tools/scratch/gen_yeul_hover_playground.mjs`) |
| 12 | **글래스 버튼** | `.pm-fbtn` (+선택 상태 `.on`=accent 채움) | **운영자 확정 260704** — 글래스모피즘 표준 버튼(`--glass-surface`+blur+`--glass-bd`+`--glass-shadow`). 홍보 지도 대시보드 전 버튼이 이 형태·신규 화면 버튼도 이 형태 우선 |
| 13 | **로더 픽토그램** | `.ol-dots`(저장 오버레이 고정 32px) · JS 팩토리 `_ldMake('dots'\|'spinner',size,speed)` · 버튼 busy 공용 `_btnBusy(btn,on,label)` | **운영자 확정(beui.dev 이식 — 원장 260708 로더 블록)** — 보관 2종만(dots=오버레이·버튼 busy 확정분, spinner=범용 예비). 색 = `currentColor` 상속(팔레트 폐쇄)·커브 = 기존 주력 계승 `cubic-bezier(0.4,0,0.2,1)`·수치 = beui 원본 배율. **카탈로그 17종 전체 = `docs/reports/260707_로더픽토그램_플레이그라운드.html`** — 추가 선별은 그 시안에서 [선택값 복사]로만. busy dots = 로딩 상태 표시라 아래 "버튼 내 SVG·이모지 금지"(라벨 장식 금지)와 별개 — 단 spinner는 SVG이므로 버튼 내 사용 금지 유지. **로딩 문구 표준(운영자 확정)** = 모든 로딩·처리 대기 표기는 dots 픽토그램 + `~중`(**말줄임 점 금지** — 점 모션은 픽토그램이 담당, `불러오는 중…`→`불러오는 중`) · 인라인 innerHTML 조립부는 `_ldHtml(size)`(본문 16 · 소형/로그인 12~14 · status 라벨 10) · 예외 = 캘린더 첫 로딩(스켈레톤 확정본 유지) |
| 14 | **입력칸 클립** | `.iobtn`/`.iobtn-edge`(+상태 `-pp` `-erase` `-copy` `-copy-e` `-hide` `-off` `-undo` `.ok`) · 래퍼 `.iowrap` · 폴백 다이얼로그 `.pastefb*` | **Q39 260803 운영자 「노뮤트 에디터 기능 배선」** — 전 텍스트 입력칸(text/url/search/email+textarea) 상단 테두리 걸침 26px 원형 [복사][붙여넣기][지우개/되돌리기]. 로직 = 노뮤트 nm-clip 원본 계승 · 값 = 이 표 기존 토큰만(잉크 `--neutral-text`·hover = `.icon-btn:hover` 레시피 자구 계승·신규 hex 0) · 부착 = 3층 자동(스윕+MutationObserver+focusin) · 제외 = `data-noclip` 1속성·면제표 `_CLIP_EXEMPT_SEL`(사유 필수) · 폴백 버튼 = `.btn.btn--sm` 조합 재사용. **SVG 각주(평의회8 판정 260803)**: 아래 「버튼 내 SVG·이모지 금지」는 텍스트 **라벨** 버튼 축 — 아이콘 전용 원형 버튼(#4 `.icon-btn`·이 행 `.iobtn`)은 별개(선례 = `#memo-btn` SVG 내장). 강제 = `tools/check_wiring.py` |
| 15 | **도넛 차트** | 인라인 SVG(`viewBox="0 0 100 100"` · 바깥 반지름 49 / 안 30 · 조각 사이 `--surface-solid` 1.6px 실선) + 옆 **범례**(칩 · 이름 · N명 · %) | **운영자 확정 260804** 「차트를 원형으로 하는게 더 맞는듯」 — 시안 `docs/reports/260804_고객분석_원형차트_플레이그라운드.html`에서 3형태×2배색을 비교해 **B(도넛 2개)+순위 강조 배색** 선택. 현재 1곳(고객 분석 4면·모달의 거주지 TOP·연령대 분포). **색 = 새로 안 만든다** — 순위 배색은 구 막대 규약 그대로(1·2위 `--accent` .79/.62 · 3위~ `--peach-text` 명도 사다리 .56→.26 · 나머지 몫 = `--neutral-d`), 순서형(연령대)은 나이순 단일 색 명도 사다리(.62→.24) = 같은 토큰 alpha 변주(§3.5②) + 이웃 구별을 색 하나에 안 맡김(§3.5③). **부분집합(TOP N)에 쓸 땐 「그 외」 조각 필수** — 원의 합이 전체가 되어야 원형이 성립한다(없는 값을 지어내지 않고 남는 몫을 보여준다). **범례 규약(운영자 260804 2차)**: 2열 = `grid-template-columns:repeat(auto-fit,minmax(max(168px,46%),1fr))` — 46%가 3열을 막고 168px가 좁은 칸에서 1열로 되돌린다(넓은 화면 2열·1920 1열) · 행 높이 = `clamp(11px,1.4vh,21px)`(`1fr`은 큰 화면에서 줄을 흩어놓는다) + `align-content:safe center`(넘칠 때 위부터 = 양끝 잘림 방지) · 이름 칸 64px 고정 + 수치 바로 뒤(수치를 오른쪽 끝까지 밀지 않는다) · **잉크 = `--text`, 상위 1·2위만 이름·수치 다 `--accent`**(도넛 조각 강조와 한 몸) · 지름은 칸 높이의 **80%**(`height:80%`). **기하 규약**: 지름 = 칸 높이(`height`+`aspect-ratio:1`, **min-height 금지** — 최소치가 칸보다 크면 그림이 칸 밖으로 나가 제목을 덮는다) · 절·범례 **둘 다 `overflow:hidden`**(flex 항목이 밖으로 그려져 겹치는 사고 차단) · 절은 `align-items:stretch`(범례가 늘어나야 줄이 조여진다) · 범례 줄 = 막대 행 규약 계승(`flex:1 1 15px;min/max`). 셋 다 낮은 화면 실측(1440×900·1366×768)으로 잡은 것 |
| 17 | **막대 차트 축 중략(≈)** | 흰 띠(`--surface-solid`)로 도형을 끊고 위·아래 가장자리에 물결 2줄(`--dim` · 폭 = 차트 전폭 1개) · 판정 `_bizBarBreak()` · 도형 `_bizBreakShapes()` | **운영자 확정 260804 → 260806 계약 승격**(「저렇게 차이가 심하면 **항상** 켜지게 정본에 박아라」). **발동 계약(정본)** = 「중략을 안 켰을 때 **본체(=안 잘리는 무리)의 최대 막대가 화면 최댓값의 30% 이하로 눌리면** 반드시 켠다」 — 이때 눈금 사다리 기반 보수 게이트는 우회한다(반올림 한 칸 때문에 명백한 뭉갬을 못 끊던 사고가 실재 · 260804~260806 라이브 화면이 그 상태였다). **안 켜는 조건(전부 유지)** = 음수 포함 · 표본 6건 미만 · 본체 5건 미만 · 잘림이 절반 이상 · 끊는 자리에 빈 구간 없음(잘린 최소 < 본체 최대 ×2) · 차트가 너무 짧음(plotPx<100). ⚠ **배율이 아니라 「눌린 정도」로 재는 게 계약의 핵심** — 「상위 무리 150~190 · 하위 무리 20~70」처럼 두 그룹으로 갈린 분포는 배율만 보면 2.5~3.9배지만 본체 최대가 83~126px로 멀쩡히 보이고, 그걸 접으면 상위 무리가 압축 구간에 몰려 **오독**이 된다(실측 6건 전건 이 형태). ⚠ **값은 손대지 않는다** — 접는 건 그리는 자리(y)뿐이고 눈금·막대 라벨·툴팁은 실값. ⚠ **막대 색은 장르색(`_BIZ_GENRE_COLORS`) 그대로** — 중략은 높이 축이고 색은 장르 축이라 서로 간섭하지 않는다(검증 픽스처도 장르색을 섞어 만든다 = 단색 픽스처는 「압축 구간에서 두 막대가 구분되는가」를 볼 수 없어 증빙으로 부적격). 강제 = `tools/check_break_rule.py`(정본 함수 재판정 · 발동/미발동 케이스 고정 · 킬테스트 6종) |

| 16 | **판매 추이 차트**(누계 꺾은선 + 보조 직각 스텝 누적) | Plotly 조립 `_bizDrawExhibMonthly(divId,list,hgt)` · 축 부품 `_bizMonthAxis` `_bizXDay` `_bizNiceStep` `_bizYearZones` · 색 `_bizExCol`(+예외표 `_BIZ_EX_COL`) | **운영자 확정 260805(1~6차 연속 판정 · 이 표에 고정하라는 지시로 승격)** — 「기간이 있는 사업의 판매/관람 추이」를 그리는 **유일한 형태**. 새 추이 차트를 만들 때 이 규약 밖으로 나가지 마라. **① 2층 구조** = 앞 **메인 = 전시별 누계 꺾은선**(주인공 · 숫자·툴팁 전부 이쪽) / 뒤 **보조 = 옅은 직각 스텝 누적 스택**(맥락 · 윤곽·점·라벨·툴팁 **0**, `hoverinfo:'skip'`). 운영자 어휘 = 「메인은 선그래프, 네모차트는 보조느낌」. **② x축** = 공연·교육과 같은 1월~12월(`_bizMonthAxis`) · 점 자리 = 그 달 눈금(`_bizXDay(1일)`). **③ y값 = 그 달까지 누계**(전시일일 누계 실측의 그 달 마지막 값 − 1/1 이전 기준선) · **정정으로 줄면 앞 달 값 유지**(누계는 줄지 않는다) · 상한 = `_bizNiceStep(max(메인×1.08, 스택×1.02))`. **④ 점(동그라미) = 상태의 표식** — 크기 7 · 테두리 1.6 · **종료 월(최종) 한 점만 채움**(선색 채움 + 카드 바탕 `--surface-solid` 테두리), **나머지·진행중 전부 빈 점**(카드 바탕 채움 + 선색 테두리). 운영자 「맨 마지막이 아니면 빈 동그라미로(테두리로 감쌈)」. **⑤ 보조 스택** = `stackgroup` + `fill:'tonexty'` + **`line.shape:'hvh'`** · `line.width:0` · 알파 **종료 .09 / 진행중 .12**(같은 토큰 알파 변주 §3.5② · 새 색 0) · 시작일순(먼저 시작한 전시가 아래) · 쌓는 값 = **그 달 동시에 진행 중이던 전시들의 누계 합**. ⚠⚠ **`hv` 금지** — 값이 점에서 오른쪽으로 뻗어 칸 **왼쪽 끝**에 꼭짓점이 놓인다(= 반 달 밀림 · 운영자 「반틈씩 밀리는듯」). `hvh`라야 칸이 그 달 눈금을 **정중앙**에 둔다. ⚠ **같은 바닥 겹치기(`fill:'tozeroy'` 비스택) 금지** — 그건 누적이 아니다(260805 3차 철거분). **⑥ 숫자는 셋만** = ⓐ 전시당 **꼭자락 1개**(마지막 점 = 그 전시 누계 · 10.5px · **잉크 = `--neutral-text` 흐린 검정 한 벌** — **운영자 확정 260809-2** 「응 그렇게 해주면 됨」(260809 「숫자는 색이 각기 다른 게 아니라, 조금 흐린 검정으로」의 선 끝 숫자 적용). **구 「선색」은 폐지** = 막대 값 라벨(#18)과 **한 잉크**다 — 한 화면에서 반만 성립하던 계약이 닫혔다. 어느 선의 값인지는 **자리**(그 선의 꼭자락)가 말한다 · 실측 260809-2 = 전시 반쪽 값 라벨 잉크 **4갈래 → 1갈래** · 교육 반쪽 **2 → 1**. ⚠ 보조 회색 ⓑ(9.5px `--dim`)는 **그대로** — 같은 중립 사다리 한 단 아래가 「보조」를 말하는 자리라 색 갈래가 아니다) ⓑ **월별 누적 합**(계단 칸 꼭대기 · 9.5px · **`--dim` 회색** · 메인이 이미 그 자리를 쓴 달은 생략 = 중복 0) ⓒ 나머지는 전부 **툴팁**(「N월 누계 X명 (그 달 +Y)」 + 기간·상태·총계). 달마다 값 라벨을 찍지 마라 — 운영자 「차트가 너무 헤비해짐 · 그래프에 선 올릴때만 파악되도록」. **⑦ 라벨 충돌 = 생략이 아니라 뒤집기** — 큰 값부터 자리를 잡고, 부딪히는 쪽(항상 낮은 값)은 점 **180° 반대편**으로. 양쪽 다 막힐 때만 생략. 임계 = x **30px** · y **19px**(= 10.5px 글상자 ≈13 + 숨구멍 6 · ⚠ 구판 14는 **글상자보다 작아** 17px 떨어진 짝을 통과시켜 숫자가 포개졌다). **⑧ 색 = 캘린더 전시선과 같은 것**(`_bizExCol` → `_EX_SPACE` 공간색) · 개별 지정은 예외표 `_BIZ_EX_COL`뿐(현재 = 프리뷰 `--accent` · 숨 쉬는 SUM `--c2` 주황) = **새 색 0**. **⑨ 구획 음영** = `_bizYearZones`(전시 톤 `--peach-text` · 경계 = 아직 안 끝난 사업 중 가장 이른 시작일). **현재 쓰는 곳** = 3면 하단 반반 좌(기획 전시). 예술교육 반쪽은 y축이 수강생 고정자(100~400)라 함수를 공유하지 않고 `_bizDrawSalesBars(dom='edu')` 안에서 **같은 규칙**으로 그린다 — 규칙이 갈리면 안 되는 자리다. **강제** = `tools/check_exchart.py`(하드 0) · 시안 = `docs/reports/260805_전시_직각누적_플레이그라운드.html`(만지는 튜닝판 · 프리셋 E ★ = 현행) · 전후 실측 = `docs/reports/260805_누적라벨_후.png` |

| 23 | **업데이트 알림 배지**(데이터 갱신 · 새 디자인) | 자리·유리·모서리·그림자·활자 = **정본 `.toast` 그대로 계승**(같은 클래스로 붙인다 — `class="toast show ub ub-data|ub-design"` · 하단 중앙 32px · 좁은 화면은 `.toast` 미디어 규칙이 하단 네비 위 80px로 밀어 준다) · `.toast`와 다른 건 **둘뿐** = 계속 떠 있고(자동 3초 없음) 눌린다(`pointer-events:auto`) · 색 = **잉크·테두리 토큰 변주만**(데이터 `--accent` 파랑 · 새 디자인 `--danger` 빨강) · 구현 `_showUpdateBadge(kind)` | **운영자 확정 260806-8** 「알림 토글 항상 위에 뜨는데 **아래에 뜨게** · 그냥 **반투명 글래스모피즘** · 아래 창하고 어느 정도 간격」 + 「디자인이 변경되면 **새로운 디자인 있다고 알림** — 이거는 **붉은색**으로(데이터가 업데이트될 때는 파란색으로 앎)」. ⚠ **배지를 두 개 쌓지 않는다** — 한 자리에 뜻 두 가지를 색으로 가르고, 둘이 동시에 걸리면 **빨강이 이긴다**(페이지를 새로 받으면 데이터도 같이 새로 읽히므로 파랑이 할 일을 포함한다). ⚠ **구판 폐지** = `top:14px` 인라인 `cssText` 한 벌(그라데이션 `#6366F1`·`color:#fff`·전용 그림자 리터럴) — 정본 밖 자리·정본 밖 값이라 패리티 부채였다(청산 = raw hex −2 · `check_design` baseline 1550→1548). ⚠ **감지**: 데이터 = `/api/lastmod` 폴링(기존) · 새 디자인 = 배포된 `index.html`의 **ETag**(HEAD 1회 · 첫 샘플 baseline · 45초 폴링과 복귀 훅 `nmRefresh` 공용). 디자인만이 아니라 `index.html`이 바뀐 배포 전부가 걸린다(디자인 변경은 항상 이 파일로 나간다 = 놓치지 않는 쪽) **[260806-14 운영자 확정] 새 디자인 = 자동 반영**(「새 디자인 있으면 자동으로 반영되게 해줘 그냥」) — 배포가 감지되면 **스스로 새로 받는다**. 구 계약 「방식B: 자동 리로드 X」는 **데이터 축에만** 남는다(데이터는 눌러서 다시 읽고, 디자인은 저절로 온다). ⚠ **사람 손 위에서 페이지를 뽑지 않는다** — 모달·위저드·로더가 떠 있거나 입력칸에 커서가 있으면 그 순간엔 안 바꾸고 **빨강 배지로 대기**했다가(수동 탈출구 보존) **안전해지는 즉시** 다음 폴링 틱에서 스스로 반영한다. ⚠ **되풀이 방지** = 마지막 자동 반영이 90초 안이면 자동으로 다시 하지 않는다(연속 배포·ETag 흔들림에도 무한 새로고침 0). **단위 실측 260806-14** = 창이 떠 있을 때 리로드 0·배지+대기 ✓ · 창을 닫자 다음 확인에서 **스스로 리로드** ✓(`tools/scratch/probe_updbadge.mjs` AUTO1/AUTO2) |
| 22 | **연간 차트 보수 기간 표기** | 구간 = **연간 캘린더 정본 상수 `_YC_MAINT` 재사용**(손으로 월을 다시 적지 않는다) · 표기 = 그 구간 **가운데 라벨 박스 1개**(세로 = paper .5 · 「보수 기간」 9.5px · 면 `--peach` .22 · 선 `--peach-text` .5 · borderpad 4) · 구현 `_bizMaintNotes()` | **운영자 확정 260806** 「차트 중간에 보수기간을 네모 박스로 · 각 구간 중앙 · 너무 튀지 않고 참고할 수 있게 · **메인은 차트**」. **부품·값 = 캘린더 유지보수 박스(`.yc-mt`) 살구 톤 계승** = 새 색 0(폴백 hex 금지 — 토큰이 안 풀리면 **라벨을 안 그린다**). ⚠ **배경 rect를 깔지 않는 게 계약** — 구간을 통째로 칠하면 그 두 달의 막대·구획 음영·분기 밴드를 전부 덮는다(「메인은 차트」). ⚠ **월은 정본 상수에서만** 온다 — 손으로 적으면 캘린더와 갈려 같은 화면의 두 자리가 다른 달을 가리킨다. ⚠ **`_YC_MAINT.ms` = 1-based 달 번호**다(캘린더 정본 `for(m=1;m<=12;m++)ranges[m]=[m]` · 마크업 `yc-m2` = 「2–3 FEB-MAR」 / `yc-m7` = 「7–8 JUL-AUG」) — 260806-6 구현은 이 값을 `new Date(y,ms,1)`의 **0-based 달 색인**으로 넣어 화면이 **한 달 뒤로 밀렸다**(실측 260806-7: 캘린더 2~3월·7~8월 → 차트 3~4월·8~9월 · 8월 말 라벨이 9월 공연 막대를 덮었다). 교정 = `ms[0]-1` ~ `ms[끝]`(끝값이 곧 다음 달의 0-based 색인) · **정본 구간 = 2~3월 · 7~8월**. ⚠ 상수는 **달 단위뿐** — 일 단위 시작·종료가 필요하면 그 값을 운영자에게 받아 `_YC_MAINT`에 넣는다(세션이 날짜를 창작하지 않는다). ⚠ **픽토그램 없음** — 캘린더 정본에도 아이콘이 없어 창작 금지([4]) · 쓸 아이콘이 정해지면 그때 정본에 편입한다 **게이트 = `tools/smoke_bizchart.mjs`**(260806-9 · 라벨 잉크 중심 Δ≤1px · **일정(캘린더) 달머리 == `_YC_MAINT`** 동기 실측 — 0/1-based 밀림·상수 오정정 재발 차단) |
| 21 | **차트 눈금 사다리**(값 축) | `_bizNiceStep()` = **1 · 2 · 2.5 · 5 · 10 × 10ⁿ** 만(구 1.5·3·4·6·8 폐기) | **운영자 확정 260806** 「좌측 단위 임의로 맞추지 말고 2500·5000·7500처럼 근본 있게 · (그 숫자는 예시) 지금 형태를 유지하면서 최대한 정해진 규격 안에서」. 사람은 축을 **반·반의반**으로 읽는다(1,000 / 2,500 / 5,000) — 1.5·3·6·8 계열은 `800·1,600·2,400` 같은 「세다 만 눈금」을 만든다. ⚠ 이 사다리는 **축 중략 판정(`_bizBarBreak`)의 상한 B 후보**이기도 하다 — 좁히면 발동이 달라질 수 있으므로 `tools/check_break_rule.py`(발동 7종/미발동 9종/오발동 회귀)를 반드시 재실행한다(260806 개정 시 rc=0 실측). ⚠ 「형태 유지」 = 축 중략·페이드·배경 2층·하단축 2줄은 무접촉이고 **눈금 값만** 바뀐다 |
| 20 | **연간 차트 하단축 2줄**(월 + 분기) | 1줄 = 달 가운데 「N월」(10.5px · `--dim`) · 2줄 = 분기 가운데 **「1Q~4Q」**(9.5px · **`--neutral-text`/600**(운영자 260806 「좀 더 진하게」 · 중립 사다리 한 단 위 = 새 회색 창작 0 · 위계는 크기 9.5 vs 10.5가 진다) · `yshift:-26`) · 차트↔월 간격 = `ticklen:5` + `tickcolor` 투명 · 아래 여백 `_BIZ_AXB=50` | **운영자 확정 260806** 「월 아래에 1Q부터 4까지 · 월 나온거 아래에다가 · 차트랑 월이랑 아주 조금 더 간격 · 흰 도형 간 마진은 동일하게」. **부품·값 = 구획 라벨(`_bizYearZones`의 `note`) 문법 그대로** 계승(9.5px · `--dim` · 데이터좌표 중앙 정렬) = 새 문법·새 값 0. ⚠ **차트 높이(hgt)는 안 건드린다** — 늘린 건 아래 여백뿐이라 카드 높이·바깥 마진이 불변이다(흰 도형 하단선 계약 = `smoke_layout`이 재는 축). ⚠ **그림 영역이 줄어든 만큼 축 중략의 `plotPx`도 같이 줄인다**(`hgt-(18+_BIZ_AXB)`) — 상수를 박아두면 없는 자리를 있다고 착각한다. ⚠ **분기 경계선은 긋지 않는다** — 달 경계선이 이미 그 자리에 있고, 분기 밴드(#19)가 면으로 말한다. ⚠ `ticklen`은 **선을 그리려는 게 아니라 라벨을 띄우는 자**라 `tickcolor`를 투명으로 둔다(새 선 0). ⚠ **주석 순서 함정** = `lay.annotations = zone.notes` **뒤에** concat해야 한다 — 앞에 두면 그 할당이 통째로 덮어써서 분기 라벨이 조용히 사라진다(첫 렌더 실측) |
| 19 | **연간 차트 배경 2층**(구획 음영 + 분기 밴드) | ① 구획 = 「진행 완료」 `--past-bg` │ 「진행·예정」 **그 차트 분야의 강조색** 알파(공연 `--accent` · 전시 `--peach-text` · 교육 `--green` · 세기 = `_bizZoneAlpha`가 톤 밝기로 자동 정규화) ② 분기 밴드 = **Q2·Q4에만** `--past-bg` 띠(Q1·Q3 = 흰 바탕) · 구현 `_bizYearZones()` | **운영자 확정 260806** 「공연은 진행·예정 부분을 공연 강조색으로(차트 배경) · 하얀색도 약간 덜 하얀색을 섞어서 분기별 음영을 살짝 구분되게」. ⚠ **분야색 계약**(공연 `--accent` / 전시 `--peach-text` / 교육 `--green` · §정본 기술 = `_mvBars` 6490행 주석)을 배경도 따른다 — 구판은 공연 음영까지 peach라 전시 반쪽과 같은 색이어서 「어느 분야 차트인가」가 배경에서 안 갈렸다. ⚠ 분기 밴드는 **절반만 칠한다**(교대) — 4칸 전부 칠하면 화면이 어두워지고 리듬이 안 읽힌다. ⚠ 색은 **기존 토큰 알파 변주뿐**(§3.5②) = 새 색·새 알파 0. ⚠ 완료 구획과 분기 밴드가 겹치는 자리는 한 겹 더 진해지는데, 그게 「완료 × 분기」 두 축이 같이 읽히는 자리다(의도). ⚠ 둘 다 `layer:'below'` = 막대·물결·값 라벨 아래 · 분기 경계는 기존 달 경계선과 같은 자리라 새 선을 긋지 않는다 |
| 18 | **진행·예정 게이지 페이드** | 아직 안 끝난 공연의 막대 = **위로 갈수록 사라지는 같은 색**(꼭대기 알파 `0.08` → 바닥 `0.85` · 굵기는 실선과 **동일**) · 구현 `_bizFadePlanned()`(렌더 후 SVG `<linearGradient>` 주입) · **값이 없는 자리(예정·미조인)도 같은 페이드** = `_bizFadeSlots()`(높이는 종전 자리 높이 `pY` 그대로) | **운영자 확정 260806** 「이거를 디자인 정본에 넣으셈」 — 시안 `docs/reports/260806_예정게이지_페이드x축중략_플레이그라운드.html`에서 값 확정. 근거 = Evergreen 「Fade it」(확정 안 된 값은 **옅은 같은 색**이 1순위 · 패턴필·점선은 이 차트처럼 얇은 게이지 W≈5px에서 깨진다). **색은 새로 안 만든다** — 그 공연의 장르색(`_BIZ_GENRE_COLORS`) **알파 변주뿐**(§3.5②). ⚠ **굵기로 위계를 주지 않는다**(달라지는 건 알파뿐). ⚠ **점선(파선) 자리 = 폐지**(운영자 확정 260806-7 「지금 점선을 다 저런 모양으로 대체해줘」 · 「진행중인 공연이 점선으로 **투박하게** 표현되어 있다 — 투박한 만큼 그라데이션으로 표현하려고 구현한 건데 정작 반영이 안 돼 있다」). 값이 없는 자리(`pidx` = 예정·매출 미조인·교육 일정)도 **같은 폭·같은 높이에 같은 페이드**를 칠한다 — 잉크만 바뀌고 자리·호버·라벨은 그대로(구 파선 rect 도형은 삭제). 실측 260806-7: 기획 공연 카드 파선 8 → 0 · 페이드 8. ⚠ 구판이 파선이던 이유는 「Plotly `marker.line`에 dash가 없다」는 **구현 제약**이었고, 그게 만든 네모 테두리가 이 행의 계약(「네모 테두리 없음 · 게이지 한 줄」)과 정면으로 부딪혔다. ⚠ **값이 있는 진행·예정 막대는 `_bizFadePlanned`가**(높이 0인 행은 거기서 자동으로 걸러져 `_bizFadeSlots`가 맡는다) = 한 막대가 두 번 칠해지지 않는다. ⚠ `_bizFadeSlots`는 **자리 트레이스(막대 트레이스 중 맨 마지막 = 투명 채움 호버 막대)의 점 수 ≠ 넘긴 색 수면 아무것도 안 칠한다**(엉뚱한 막대를 칠하느니 안 칠한다). ⚠ 이 행은 260806 2차 「막대 농도(상태별 opacity·테두리) 폐지」의 **개정**이다 — 다만 배경 음영 구획은 그대로 남는다(음영 = 「오늘 기준 어느 시기인가」 달력 축 · 페이드 = 「이 공연이 끝났는가」 개별 축 = 서로 다른 축). ⚠ 구현이 렌더 후처리인 이유 = Plotly `marker.color`는 단색만 받는다. 트레이스를 알파 사다리로 쪼개면 막대 하나가 N개 도형이 되어 값 라벨·툴팁·히트존이 갈라진다 → 렌더된 막대의 `fill`만 그라데이션 참조로 교체(도형 수·좌표·라벨·툴팁 무접촉). ✅ **2단 표기 = 완결**(260808-2 · 구 「미완」 항 해소). **운영자 확정 260808-2** 「지금 판매중인 공연이 **예상 판매율**을 그라데이션으로 표현하자는 거였는데 (지금 이해가) 조금 어긋나 있다」 + 「각 판매 공연 누르면 **예상으로 몇 개 팔립니다** 뜨는 게 있어 — 그거대로」. 계약 = **판매중 막대 = 팔린 만큼 진한 실선 + 예상 만큼 페이드 한 칸**. ⚠ 예상값의 원천은 **새로 만들지 않는다** — 레일 카드 「AI 예상」·카드 차트 종료 예상점이 쓰는 그 엔진(`_bizFcSeat()` = 지금 판 좌석 + `fcRate`(「예측제외」 반영 일평균) × 판매 종료일까지 남은 날 · **총오픈석 상한**)뿐이고, 게이트도 그대로 상속한다(실측 7건 `_SRAIL_FC_MIN` 미만 · 남은 3일 미만 · 집계 전 · 스테일 종료일 = **안 그린다**). ⚠ 높이는 **배율**(`예상석 ÷ 지금 판 좌석`)을 그 막대 값에 곱한다 — 축이 매출이면 「지금 단가 유지 기준」이라는 뜻이라 **툴팁에 그 문장을 박는다**(값 창작으로 읽히지 않게). ⚠ 구현 = 실막대 위에 얹는 **별도 트레이스**(`base` = 실판매 꼭대기 · 채움은 투명으로 태어나 `_bizFadeProj()`가 같은 페이드를 칠한다) — 실막대의 좌표·값 라벨·툴팁·히트존은 **무접촉**. ⚠ **트레이스 순서 계약** = 실막대 → 예상 칸 → 자리 트레이스(맨 마지막). `_bizFadeSlots`가 「맨 마지막」으로, `_bizFadeProj`가 「index 1」로 찾으므로 순서를 바꾸면 **둘 다 어긋난다**(둘 다 점 수 ≠ 색 수면 아무것도 안 칠하는 자기검사는 유지). ⚠ 예상 꼭대기가 축 중략 상한을 넘으면 **축을 넓힌다**(자르면 「예상이 낮다」로 오독) · 눈금(`tickvals`)·중략 발동 계약은 무접촉. ⚠ **값 라벨 자리 = 그라데이션 칸 꼭대기 위**(운영자 260806-11 「그 끝 점 위에 그라데이션이랑 중첩 안 되게」) — 구 260808-2의 「옆으로」(xanchor left · xshift 9)는 **폐지**(페이드 칸 안에 잠기던 문제가 자리를 페이드 밖으로 올리며 해소 = 이제 다른 값 라벨과 완전히 같은 벌). ⚠ **티켓 오픈 전 사업은 이 차트에 아예 안 그린다**(운영자 확정 260808-2 「차트에는 **판매 했었던 ~ 판매 중인 것만** 나오면 됨」) = 구 260805 「예정도 넣어줘」의 **차트 한정 철회** — 거르는 자리는 `_bizDrawSalesBars` 입구 한 곳(KPI 「+예정 N」·우 열 상세 표는 같은 `list`로 종전대로 센다) · 교육 반쪽(`dom='edu'`)은 무접촉. 실측 260808-2(프로브 `tools/scratch/probe_fc2tier.mjs`) = 값 없는 페이드 자리 **7 → 0** · 예상 2단 **0 → 5**(판매중 6건 중 100% 판매분 1건은 상한이라 자동 제외) · 콘솔 에러 0 · 전후 `docs/reports/260808-2_예상2단_전후.png` ⚠ **막대 트레이스가 둘 이상이면 `barmode:'overlay'`가 필수**(운영자 260806-9 「차트가 일단 삐뚤어져 있어」) — Plotly 기본값 `group`은 트레이스를 좌우로 갈라 놓아서, **같은 사업의 실막대와 예상 칸이 다른 x에 서고 막대가 계단처럼 어긋난다**(실측 260806-9: 예상 칸이 실막대보다 **+3.53px** 오른쪽 · 막대 폭 5px = 거의 한 칸 옆 · 실막대 자신도 제 날짜에서 하루치 밀려 있었다). 고친 뒤 Δ**0.00px**. 이 레포 연간 실적 차트가 같은 사고로 이미 `overlay`를 쓰고 있다 = **그 정본 문법 계승**(새 값 0). 전후 = `docs/reports/260806-9_막대어긋남_전후.png` **게이트 = `tools/smoke_bizchart.mjs`**(260806-9 · 파선 0 · 겹칸 x정렬 · `barmode:'overlay'` 실측) ⚠⚠ **[260813] 페이드는 「한 번 칠하고 끝」이 아니다 — Plotly가 다시 그릴 때마다 다시 칠한다**(운영자 「**다시 그라데이션 수치가 빠졌거든?** 그거가 들어갔을때의 위치에 각 숫자가 있는거 같음」). 예상 칸·빈 자리 막대는 **투명으로 태어나** 덧칠이 유일한 잉크라, 재렌더 한 번에 `fill`이 투명으로 돌아가 **그라데이션만 증발하고 숫자·자리는 남는다**(= 운영자가 본 화면 그 자체). 실측 260813(1600×900) = `Plotly.relayout(차트,{height:…})` 직후 칠해진 칸 **5 → 0** · `Plotly.Plots.resize` 직후 **0** · 그런데 `defs`의 `<linearGradient>`는 **5개 그대로**(그래서 defs를 세는 검사는 이 사고를 못 잡는다). 그 relayout을 부르는 자리가 상시로 있다 — `_bizFitSeason()`(차트를 유리박스 안선에 맞추는 fit)이 **창 크기 변경·책 넘김 500ms 뒤 1패스·레일 렌더**마다 다시 돈다. 첫 그리기 직후엔 「높이가 20px 넘게 바뀌면 다시 그린다」가 우연히 되살렸지만 그 뒤의 fit(2~20px)은 **덧칠만 지우고 다시 그리지 않는다**. → 계약 = **`plotly_afterplot` 훅에 덧칠 recipe를 걸어 둔다**(호출자를 쫓지 않는다 — 새 호출자가 생길 때마다 샌다). 리스너는 **갈아 끼운다**(remove → on · 한 화면에서 두 번 그려지므로 낡은 recipe가 새 그림을 칠하면 엉뚱한 막대가 물든다). 덧칠은 `path.style.fill`만 건드려 재렌더를 부르지 않는다 = 되먹임 없음. **게이트 = `smoke_bizchart.mjs` 계약 ⑥**(relayout·resize 뒤 **칠해진 칸 수**가 줄면 FAIL — defs 개수로 재지 말 것) · **킬테스트 260813 실증** = 직전 머지본(`225bd1b`) index.html에 걸면 rc=1(relayout 6→0 · resize 6→0 전건 검출) · 고친 뒤 rc=0. **[260806-13 운영자 확정] 2단 위층(예상 판매분) 꼭대기 알파 = `0.35`**(`_BIZ_FADE_TOP_PROJ`) — 그 칸은 **끝에 값 라벨을 이고 있어서** 0.08(「거의 사라짐」)이면 끝이 안 보이고 숫자만 뜬다(실측 260806-12 = 라벨 아래 ~20%가 사실상 투명 · 3안 0.20/0.35/0.50 중 선택). **진행·예정 게이지·값 없는 자리는 0.08 그대로** — 그쪽은 끝에 숫자가 없어 「사라지는 끝」이 곧 뜻이다. ⚠ **라벨 값 = 그 자리가 뜻하는 값**(260806-13 「수치에 맞게끔 차트 배선」) — 예상 꼭대기에는 **예상값**을 적는다(구판은 그 높이에 실판매값을 적어 숫자와 높이가 어긋났다). **[260813-4 운영자 확정] 예상 칸이 얹힌 막대의 글자 = 두 줄** — 「현재 수치를 그냥 표기 · 예상 수치를 () 안에 · **현재 수치를 위에, 수직맞춰서 예상 수치를 그 아래로** · 예) 현재 4에 7이 예상되면 `4` 다음 줄에 `(7)` · 단 4랑 7은 **수직 유지** · **(7)은 더 연하게 - 기울임체**」. 윗줄 = **현재치**(본체 잉크 `--neutral-text`) · 아랫줄 = **`(예상치)`**(중립 사다리 한 단 아래 `--dim` + `<i>` 기울임 = #16 ⓑ가 이미 못 박은 「보조」 자리 · **새 색 0**). ⚠ **수직 맞춤은 주석 기본 정렬이 그대로 준다**(`annotation.align` 기본 = center — `(7)`의 가운데가 곧 `7`의 가운데) = 별도 좌표 계산 0. ⚠ 두 값이 **반올림해서 같으면 아랫줄을 안 붙인다**(페이드 칸이 1px 미만이면 보여 줄 「범위」가 없다 · 글상자만 두 배가 된다). ⚠ 글상자는 **긴 줄이 폭**(`_bizLabW`)·**줄 수가 높이**(`_bizLabH` = 13 + 줄당 12.5)를 정하고, 자리잡기 세로 임계도 그 높이로 잰다(한 줄끼리는 여전히 19 = 회귀 0). ⚠ 축 여유도 **두 줄 몫**(16 → 28.5px)으로 넓힌다 — 안 넓히면 윗줄이 그림 밖으로 잘린다. **구 260813(한 줄 `예측치(현재치)` = `70(33)`)은 폐지** — 읽는 차례가 뒤집혔다(먼저 오는 수 = 지금 · 괄호 = 앞으로). 그 앞판(260806-13 「막대에 숫자를 둘 달지 않는다」)의 개정은 그대로 유효 — 구 260806-13의 「실판매값은 툴팁이 말한다 = 막대에 숫자를 둘 달지 않는다(번잡)」는 **개정**된다. 이유 = 툴팁은 마우스를 올려야 나오는데 그라데이션 칸은 「**어디서 어디까지가 예상인가**」가 곧 그 칸의 뜻이라, 바닥값이 화면에 없으면 페이드 시작점을 눈으로 되짚어야 했다. 괄호 한 덩이 = **자리(예상 꼭대기)는 그대로** 두고 글자 안에서 두 값을 잇는다 = 라벨 기구·간격·잉크(260809 「한 벌」) **무접촉**. ⚠ 두 값이 **반올림해서 같으면 괄호를 안 붙인다**(`135(135)` 금지) — 그 자리는 페이드 칸이 1px 미만이라 보여 줄 「범위」가 없고, 같은 수를 두 번 적으면 글상자만 넓어져 옆 숫자를 밀어낸다. ⚠ **글상자 폭(`_bizLabW`)에 괄호 몫을 더한다**(짝당 3.5px = 10.5px 활자 실측 `getComputedTextLength`: 숫자 5.84 · 쉼표 2.92 · 괄호 3.50 — 앞 두 항의 6·3이 이미 그 실측의 반올림이라 **같은 자로 잰 것뿐 · 새 수치 0**). 안 더하면 글상자가 7px 좁게 잡혀 **겹친 채로 「안 겹쳤다」고 판정**된다(겹침 회피의 유일한 자가 그 함수다). 툴팁은 종전 그대로(좌석·매출 두 축을 다 말하는 자리는 거기다) · 실측 260813 = 라벨 겹침 **0**(글상자 교차 0쌍) · 전후 = `docs/reports/260813_예상라벨_그라데이션_전후.html` **[260809 운영자 확정] 값 라벨 = 「한 벌」**(「각각 다 **숫자 간격이 다르거든**? 그 부분 해결해주고, **모든 올라와 있는 차트가 다 숫자가 나오게** 해줘 · 숫자는 **색이 각기 다른 게 아니라, 조금 흐린 검정으로**」). 계약 넷 = ⓐ **기구 하나** — 전부 주석(`annotation`)이다. Plotly 막대 텍스트(`text`+`textposition:'outside'`)는 **폐지**(그 방어로 달아 뒀던 `constraintext:'none'`도 같이 · 막대 텍스트가 없으면 줄일 것도 없다). 기구가 둘이면 **간격이 둘**이 된다 — 실측 260809 = 막대 텍스트 Δ**0.00px** vs 주석 Δ**5.04px**. ⓑ **세로 간격 = `_BIZ_LAB_DY`(3) 한 값** · 활자 `_BIZ_LAB_FS`(10.5). ⚠ 겹침을 **세로로** 풀지 마라 — 그 순간 이 계약이 깨진다. ⓒ **생략 금지**(값이 있으면 숫자가 있다). ⚠ **[260813-3 운영자 확정] 「겹치면 가로로 비킨다」는 폐지** — 「**상단에 무조건 숫자가 있어야 함 · 차트 옆에 있는건 실패임 · 단 가려질 경우 흰색 반투명**」. 이유 = 옆으로 비킨 숫자는 **「제 막대의 값」으로 안 읽힌다**(숫자의 뜻은 자리가 진다 — 자리를 옮겨 겹침을 푸는 건 뜻을 버리고 모양을 얻는 것). → 값 라벨은 **언제나 제 막대 정중앙 위**(`xshift` 없음 · 상수 `_BIZ_LAB_DX` 삭제 · `_bizLabFit`은 이제 밀지 않고 **겹치는지만 알려 준다**), 겹침은 아래 ⓕ 판이 진다. **대가(실측 260813-3)** = 사업이 9일 안에 3건 몰린 달에서 라벨이 실제로 포갠다(포갠 쌍 2 · 전건 판 · 가장 작은 값이 두 이웃 숫자 밑에 깔린다) — 세 규칙(생략 금지 · 세로 이동 금지 · 가로 비킴 금지)을 다 지키면 물리적으로 자리가 없다. **운영자가 그 대가를 알고 고른 값**이다(대안 = 비킴 상한 확대 / 몰리면 괄호 생략 / 0 라벨 숨김 — 전부 부결). 글상자 = 폭 `_bizLabW`(자릿수×6 + 쉼표·소수점×3 + **괄호×3.5** + 숨구멍 6 = 구 상수 29 「4자 라벨 + 숨구멍」을 자릿수별로 편 것 · 전부 10.5px 활자 실측 `getComputedTextLength`의 반올림 = 새 수치 0) · 세로 임계 **19**(#16 ⑦과 같은 자). 구판은 x 29px **하나만** 보고 지워서 값이 있는 막대 6개가 숫자 없이 서 있었다. ⚠ **가림 판정은 잉크 폭**(글상자 − 숨구멍 6)으로 잰다 — 숨구멍은 「안 닿게」 붙인 여유라 그것까지 세면 3px 떨어진 라벨에도 판이 깔린다(실측 「16」). **ⓕ [260813-2 운영자 확정] 가려진 라벨 = 흰 반투명 판**(「차트에 가려지면 흰색 반투명 배경을 깔자」). 대상 = ⓐ 키 큰 이웃 **막대**가 관통 ⓑ 몰린 달에서 **옆 숫자**와 포갬 — 둘 다. **가려질 때만** 깐다(전 라벨에 깔면 안 가려진 15개까지 흰 상자를 이고 서서 화면이 시끄러워진다). 색 = `--surface-solid` **알파 0.88 변주**(§3.5② · 이 파일 판매 차트 주석들이 이미 쓰던 값 = 계승·새 값 0 · 소스는 `_yrHexA(토큰,0.88)`이라 raw hex 0) · `borderpad` 1 · `borderwidth` 0. ⚠ 판을 깔면 Plotly가 글상자 높이를 다른 자로 잡아 같은 `yshift`에서 글자가 **~2px 내려간다**(실측 = 판 없는 라벨 간격 4.6~5.5px · 판 붙이면 2.9~3.5px) → `yshift`를 `borderpad`만큼 **올려** 되돌린다(ⓑ 「간격 한 값」 유지 · 실측 = 보정 후 Δ0.86px = 판 없던 때와 같은 값 · 보정 없음 Δ1.04 · 반대 방향 보정 Δ2.61 = FAIL). ⚠ 겹친 쌍은 **양쪽 다** 판을 인다(한쪽만 이면 아래 깔린 숫자가 그대로 묻힌다) · 싣는 순서는 **작은 값부터** = 겹치면 **값이 큰 쪽이 위**로 온다. **ⓖ 값 라벨 중심 == 제 막대 정중앙**(Δ ≤ 2px · 260813-3의 계약을 픽셀로 굳힌 자리). **ⓗ 예상 줄 `(N)` = 본체보다 **밝은** 잉크 + **기울임**(260813-4 · 「연하다」는 눈이 아니라 **상대 휘도**로 잰다 · 게이트가 색값을 안 적는 이유 = 정본은 토큰 하나). **[260813-2 운영자 확정] 반올림해서 0이 되는 값만 `0.n`**(소수 한 자리) — 「0일때는 0.n으로 별도로 표기」. 왜 = 「0」 한 글자가 두 가지를 같은 얼굴로 말했다(ⓐ 진짜 0 = 티켓은 열렸는데 아직 0원 ⓑ 반올림 전 작은 값 = 40만원). 막대 높이도 둘 다 사실상 0이라 그림으로 안 갈린다. 구 260806-11 「항상 반올림해서 소숫점 없이」의 **0 한 칸 예외**(1 이상은 종전 그대로 정수 · 교육·관객수 축은 정수 ≥1이라 이 가지에 안 걸린다 = 「0.0명」 안 생김). ⓓ **잉크 = `--neutral-text`(#6B6B7B) 한 벌** — 260805 「값 라벨 = 그 막대 색」 판정의 **개정**(막대와 숫자는 자리로 이미 묶여 있다 · 중립 사다리 한 단 = 새 회색 창작 0 · 분기 라벨 260806-6이 이미 쓴 그 자). ✅ **꺾은선 꼭자락 숫자까지 확장 완료**(운영자 승인 260809-2 「응 그렇게 해주면 됨」) — 구 「막대만」 한정은 **해소**됐다. 이제 3면 세 카드(공연 막대 · 전시 꺾은선 · 교육 막대+꺾은선)의 값 라벨이 **한 잉크**(`--neutral-text`)다(#16 ⑥ⓐ 개정 · 게이트 `check_exchart` ⑧). 전후 = `docs/reports/260809_공연차트_값라벨_전후.html` · `docs/reports/260809-2_선끝숫자_흐린검정_전후.html`. ⚠ 축 상한(`axTop`)은 **한 곳에서만** 정한다 — 라벨 겹침을 픽셀로 재려면 빈 자리 높이(`pY`)·`yaxis.range`와 같은 자를 봐야 한다(구판은 두 곳이 갈려 있었다). **게이트 = `tools/smoke_bizchart.mjs` 계약 ⑤**(ⓐ 막대 텍스트 0 · ⓑ 간격 Δ≤1.5px · ⓒ 숫자 없는 막대 0 · ⓓ 잉크 1갈래 · ⓔ 포갬 = **정보만**(260813-3 개정 — 비킴이 폐지돼 포갬은 일어난다) · **ⓕ 가려진 라벨은 전건 판 · 안 가려졌는데 판을 이면 잉여 = FAIL** · **ⓖ 라벨 중심 = 막대 중심 Δ≤2px · **ⓗ 예상 줄은 더 밝은 잉크 + 기울임**) · ⚠ ⓑ 간격은 **제 막대**와만 잰다(±2.5px) — 구판은 ±14px 안의 아무 막대나 골라서, 가려진 라벨이 남의 막대와 재져 간격이 −3px로 튀었다 · **킬테스트 260813-2/3/4 실증 6/6** = 판 제거 rc=1(ⓕ) · 전 라벨에 판 rc=1(ⓕ 잉여 16개) · 세로 보정 뒤집기 rc=1(ⓑ Δ2.61px) · 라벨 ±9px 비킴 rc=1(ⓖ 13개) · 예상 줄에서 기울임·연한 잉크 제거 rc=1(ⓗ 5개) · 두 줄을 한 줄로 되돌림 rc=1(ⓗ 5개) · **킬테스트 260809 실증** = 직전 머지본(`ed8325e`) index.html으로 돌리면 rc=1(4건 전건 검출 = 막대 텍스트 4 · Δ5.49px · 숫자 없는 막대 9 · 잉크 4갈래) |
| 24 | **대시보드 리빌**(창·차트 등장) | 모양 = CSS **`.rv` 한 벌**(`rvUp` = 상승 14px + 아래→위 클립 + 페이드 · `.22,.9,.36,1` · **420ms** · `backwards`) · 순번 = 컴포넌트 로컬 변수 `--rv-i`(× `--rv-step` **55ms** · 상한 `_RV_CAP` 5) · 배선 = **3층 자동부착** `_rvSweep(보드본문)`(직계 자식 위→아래 · **빈 상자는 건너뜀** = 차트 자리) + `_rvChartMo`(Plotly `.plot-container` 삽입 감지 → 그 차트만) + IntersectionObserver(화면 밖 차트 = 보일 때) · 감속 선호 = 전면 off | **운영자 지시 260810** 「대시보드에 있는 창, **띄워지는 순간, 그려지는** … 차트도 딱 떴을때 **아래에서 위로 딱 올라오고(각각의 차트에 따름)** … 현황판이 **실시간으로 있는 액티브**」. **새 모션 문법 창작 0** = 이 파일 기존 4부품의 일반화 — 상승 `fadeUp`(12px·차트 인라인 2곳)·`secPop`(14px·구 `_salesSectionPop`) / 아래→위 클립 = `ddFold`(위→아래 말림)의 **방향 반전**(가로 쌍 = 기존 `stReveal`) / 커브 = `secPop` 자구 / 촤르르 = 구 `_salesSectionPop` 0.22s·`.ry-yr-dd button` JS delay 문법. **값만** 2026 UIUX 표준으로 조임(항목 간 30~80ms 권고 → 55ms · 개별 <500ms 권고 → 420ms · 진입 ease-in 금지). ⚠ **총 길이 상한 0.7s는 계약이다** — `backwards`는 지연 동안 from 상태(translateY 14px)로 **정지**해 있어서, 지연이 250ms×2를 넘으면 `smoke_layout`의 정착 판정(250ms 간격 2연속 동일)이 그 정지를 「정착」으로 오인해 잰다. `_RV_CAP` 5가 이 계약을 지킨다. ⚠ **인라인 `animation`이 있는 자리는 안 건드린다**(책 1면 `#yrm-chart` fadeUp) — 인라인이 클래스를 이겨 어차피 안 먹고, 그 면은 기하 게이트가 잰다. ⚠ 적용 = **보드(창) 4종**(판매 현황·판매 분석·연간 누적 실적·연간 실적) + **Plotly 차트 전역**. 폼·도구 창(티켓 등록·블로그 도구)은 **제외** — 입력 한 글자마다 재렌더라 매번 다시 올라온다. 실측 = 리빌 부착 9/8/17개·차트 3/1개·**안착 후 잔여 스타일 0**·pageerror 0 · 시안 `docs/reports/260810-2_대시보드_리빌_플레이그라운드.html` |

드롭다운은 공용 헬퍼 `_ddOpen(anchor,builder,minW)`/`_ddCloseAll()`(id=`dd-pop`)로만 — 동시 1개 열림 규약.

**🚫 버튼 내 SVG·이모지 금지 (운영자 확정 260704)** — 버튼 라벨은 **텍스트만**. 아이콘이 필요하면 버튼 밖(제목·칩·안내 문구)에 배치.
**예외 3(260805-28~29 운영자 「여기서 찾으셈」 → 「그거로 전 구간 통일시켜주면 됨」 승인)**: **픽토그램 = `_NM_ICO` SSOT 한 벌**(운영자 레포 `muteno/nomute-editor` `viewer/nm-svg.js`에서 **값 그대로** 이식 · 현재 6종 `download`·`extlink`·`chevL`·`chevR`·`check`·`undo`). 자리마다 SVG를 **손으로 다시 타이핑하지 않는다** — `_nmIcoSweep()`이 화면의 문자 글리프 버튼(`‹ › ↗ ⟲ ✓`)을 훑어 SSOT 값으로 바꾼다(부착 = §2 #14 입력칸 클립의 **스윕 + MutationObserver** 문법 계승 · 정적 HTML·JS 생성분 공통). 크기는 `.nm-ico{width:1.15em}`로 **자리의 글자 크기를 상속**한다(자리마다 수치를 다시 정하지 않는다 · 머리줄 13px → 14.95px ≈ 시안 채택 15px). ⚠ **`✕`(U+2715)는 지도에 없다** — 게이트 ⑦ 하드 0 축이라 절대 치환 금지. ⚠ `─`·`▲`·`▼`·`＋`도 제외(선/면이 이미 두툼 + SSOT 대응 없음). 머리줄 조작 알약(`_mheadBtn`)은 **아이콘 + 글자 라벨 병기**도 허용 — 단 아이콘은 **`_NM_ICO`(운영자 레포 `muteno/nomute-editor` `viewer/nm-svg.js` 아이콘 SSOT 19종에서 값 그대로 이식)** 에 있는 것만. **한 획도 새로 그리지 않는다** · 그 SSOT에 없는 뜻은 **글자 라벨로 간다**(실제 사례 = 「새 창」 = SSOT에 외부링크 아이콘 없음 → 글자 유지). 외부 아이콘 사이트(Iconscout 등)는 아이콘마다 라이선스가 갈려(무료/프리미엄·표시 의무) **미채택**. 근거 = NN/g 「아이콘만 두면 예측정확도 60% · 라벨 병기 88%」 · 이 레포는 이미 노뮤트에서 §2 #14(nm-clip)·§3.5(색 거버넌스)를 이식한 전례가 있다.
신규/수정 버튼부터 즉시 적용. (기존 레거시 버튼의 이모지·SVG는 동결 부채 — 전면 청산은 §6와 함께 운영자 판단 대기.)

---

## 3. 묻고-편입 프로토콜 (기틀에 없는 게 필요할 때)

1. **멈춘다.** 임의로 raw hex·새 클래스·새 :root를 만들지 않는다.
2. **운영자에게 질문**: ① 왜 필요한지 ② 가장 가까운 기존 후보(토큰/컴포넌트)와 왜 부족한지 제시.
3. **승인되면 즉시 기틀 편입** (3점 세트):
   - `:root` 해당 블록에 토큰 추가 (raw hex는 토큰 정의부에만 존재)
   - 이 문서 인벤토리/토큰 표에 등재
   - `tools/check_design.py` baseline 갱신 + **사유 주석** (예: `# 1827→1829 = PR#XX 위험 배너 신설 +2(2607XX)`)
4. 거절되면 기존 후보로 구현.

## 3.5 색 거버넌스 로직 — 팔레트 폐쇄형 + 근접색 규칙 (노뮤트 방법론 이식, 운영자 확정 260704)

> 다른 프로젝트(노뮤트)에서 확립된 **색 관리 로직**을 이 레포에 이식. 색값이 아니라 **로직**만 계승 —
> 여수 팔레트(§1 토큰)는 그대로 두고, 색을 *다루는 규칙*을 이 방법론으로 정밀화한다.

**① 팔레트 폐쇄형** — §1의 `:root` 토큰이 **팔레트의 전부**다. 팔레트 밖에서 색을 들여오거나, 기존 칩과
**비슷한 색을 새로 만들어 칠하는 것을 금지**한다(유사색이 난립하면 브랜드가 무너진다 — 이 **폐쇄성이 브랜드의 핵심**).
각 역할색은 **딱 하나**. 새 색이 정말 필요하면 임의 생성 말고 **운영자 확인 → 정문(§3 3점 세트)**.

**② 근접색 규칙 — 드리프트 방지 ≠ 유사색 사냥** (이 구분이 핵심):
- ⛔ **금지 = 팔레트에 *없는* 새 색 즉석 창작·반입** ("비슷하니 대충 이 hex" = 드리프트). 새 UI는 기존 토큰 `var()` 계승.
- ✅ **정상 = 팔레트 *안* 유사색 공존** (역할이 다르면 비슷해도 됨) — **위반 오판·강제 병합 금지**.
  (→ §6 부채의 '빨강 2종·`--kakao` 이중정의' 등도 *역할이 다르면 억지 통합 대상 아님*. 청산은 시각검증 후 운영자 판단.)
- ✅ **정상 = 같은 토큰 alpha 변주** (hover·틴트·지도 색칠 20% 등) = 신규색 아님(게이트 raw hex도 안 늘어남).
- 정 새 색이 필요하면 = 승인 후 `:root` 토큰 추가(정문).

**③ 인접 카테고리 구별 = 색 하나에만 의존 금지** — 두 색이 지각적으로 가까우면(warm 쌍 등) hue만으로 나누지 말고
**채움 명도 사다리 + 테두리 + 글자색(유채색↔흰)** 등 **비색 단서를 병행**한다 → 색맹·작은 칩에서도 구별, 신hex 0.
(패턴: 강조=진한 배경+흰 글자 / 약=옅은 배경+유채색 글자. = 명도·글자색 이중단서로 갈라짐.)

**홍보 지도 색 역할 (폐쇄 집합 — 이 토큰만 사용):** 예울마루=`--accent` · 협력기관 DID=`--c5` · 전광판=`--c4` ·
현수기·입간판=`--c2`. 도시 색칠(baked PNG, index.html hex 무관): 여수=살몬(`--peach` 계열)·순천=파스텔그린·광양=파스텔핑크.
⚠️ **여수 살몬 ↔ 광양 핑크 = 근접 warm 쌍** — 구별 강화 필요 시 위 ③의 명도/글자 사다리 적용(운영자 요청 시).

## 4. 게이트 동작 (3층 방어)

| 층 | 시점 | 파일 | 실패 시 |
|---|---|---|---|
| 1 | 세션 시작(startup/resume/compact) + UI 어휘 프롬프트 | `.claude/hooks/design_digest.py` | (주입만 — 차단 없음) |
| 2 | `index.html`·`signage/*.html` Edit/Write 직후 | `.claude/hooks/design_gate.py` | exit 2 → 같은 턴 자가수정 유도 |
| 3 | `git commit` | `.githooks/pre-commit` | 커밋 차단 |

- 공용 검사기: `python3 tools/check_design.py` (①raw hex 총량 ②:root 블록 수 ③새 고아 토큰 ④새 이중 정의 **⑤텍스트 대비 WCAG AA 4.5:1 ⑥잰크 전이 ⑦정본 부품 일치**)
  - **⑦ = 260805-16 신설(운영자 지적 「x버튼이 다른 디자인 정본하고 아예 안 맞는다」 → 2차 「게이트로 저런것들 일괄 처리해줘」).** ①~⑥은 전부 「**색·수치**가 토큰인가」 축이라 「**정본 컴포넌트가 정본 부품으로 조립됐나**」를 한 번도 안 물었다 — 실제로 홍보·점검 모달 닫기 X가 앱의 나머지 52곳(`✕`)과 달리 `×`로 나갔는데 **①~⑥ 전건 통과**했다(색·자리·대비가 전부 정본이었으니까). 구조적 원인 = `.modal-x`는 **CSS만 정본**이고 **글자는 문자열 복붙** = 내용에 SSOT가 없었다. **판정(둘 다 하드 0)** = ⓐ 글자 `✕`(U+2715) 하나 · ⓑ `title`·`aria-label` 한 벌. **대상 = 「라벨이 X 한 글자인 버튼」 전부 · 클래스 무관**(1차는 `class="modal-x"`만 봐서 **인라인으로 직접 그린 X 버튼 4곳**을 영원히 못 잡았다 — 실측상 그 4곳이 전부 `×`에 title·aria 0이었다). **일괄 청산 실측 = 48곳 정본화**(글자 `×`→`✕` 7 · 접근명 41 — 기존 `title`을 **그대로 미러**해 넣었지 뜻을 지어내지 않았고, title·aria가 둘 다 없던 8곳만 실측으로 뜻을 확인해 「닫기」 부여). 잔여 0이라 래칫이 아니라 **하드 0**. ⚠ 접근명 **값**은 게이트가 안 정한다 — 같은 X라도 자리마다 뜻이 다르다.
  - **⑧ = 260805-32 신설(운영자 「ㄱㄱ」 승인).** ①~⑦은 전부 「**소스에 글자가 있나**」를 묻는다 — 그래서 **브라우저가 그 규칙을 버렸는지**는 아무도 안 본다. 실사고 = 주석 여는 `/*` 하나가 빠지자 파서가 복구하며 **바로 뒤의 정본 규칙(`.modal:has(>.mhead)>.modal-x{…}`)을 통째로 버렸고**, 그 상태로 ①~⑦도 `smoke_modal_head`(패리티)도 **전건 PASS**였다 — 패리티는 **모든 모달이 똑같이 깨져** 있으면 「다 같다」로 통과하기 때문이다(패리티 축의 구조적 사각). **판정(전부 하드 0)** = ⓐ 닫히지 않은 `/*` · ⓑ 짝 없는 `*/` · ⓒ 중괄호 불균형 · ⓓ **주석이 진짜 규칙을 삼킨 자국**(닫는 `*/`만 지우면 주석이 다음 `*/`까지 늘어나 **표시 짝은 다시 맞고** ⓐⓑ로 안 잡힌다 — 그 사이 규칙만 조용히 사라진다). ⓓ의 지문 = 주석 본문에 **`{` 앞이 ASCII 셀렉터 글자뿐인 줄**(한글이 섞이면 산문이지 규칙이 아니다 · 이 레포 주석의 백틱 CSS 인용도 여기서 걸러진다). **킬테스트 = 이 파일의 CSS 주석 404개 전수** — 여는 `/*` 제거 **404/404** · 닫는 `*/` 제거로 규칙을 실제로 잃는 345건 **345/345** 검출 · 현행 파일 **오탐 0**. ⚠ 문자열·`url()` 안의 `/*`·`*/`는 세지 않는다.
  - **⑤⑥ = 260804 「impeccable 적용점」 8인 평의회 선별이식.** ①~④는 전부 「색이 토큰으로 정의됐나」를 묻고 「그 색을 사람이 읽을 수 있나」·「그 전이가 프레임마다 리플로를 내나」는 한 번도 안 물었다 — 게이트 grep 실측 `contrast|wcag|4.5|luminance` **0히트**. ⑤ 판정 = 같은 `{}` 블록에 `color`·`background`가 **둘 다 리터럴 hex**인 쌍만(공존 확정분만 = 토큰 크로스곱 추정 배제) · **signage 제외**(관객용 대형 디스플레이 = 기준 상이 · `.status` #333·opacity:.3 = 의도적 비열람 디버그 각인). ⑥ 판정 = `transition` 값의 width/height/padding/margin(min/max 포함) · 주석 줄 제외 · signage 동일 기준. 둘 다 **래칫**(하드 0 금지 — pre-commit이 staged 무관하게 무조건 돌아 「위반 0」 기준은 docs 커밋까지 막는다). 임계 4.5:1 = WCAG 2.x AA 국제 표준 상수 = **새 값 창작 아님**. ⚠ impeccable 본체(설치·훅·스킬·23커맨드)는 **미도입** — 평의회 실측상 59룰 통째 적용 시 두 레포 경고 690건 중 **574건(83.2%)이 글래스 `.pm-fbtn`(운영자 260704 확정)·브랜드 인디고 `--accent`(§0① 불변) 등 정본**이라 위양성이고, `colorize`/`bolder`/`delight`/`overdrive`는 디자인 창작 = 지침 [3] 정면 위반이다. 실측 스냅샷 = 대비 미달 **49**(같은블록 쌍 95 중) · 잰크 **15**(index) / **0**(signage).
- pre-commit 활성화는 세션 시작 훅이 `git config core.hooksPath .githooks`로 자동 수행(멱등).

### 4.1 레이아웃 계약 게이트 (260802f 신설 — 「블러 칸·흰 라인은 항상 이 상태로 고정」)

색·토큰이 아니라 **기하**를 잠그는 4번째 검사기: `node tools/smoke_layout.mjs [폭 높이]`(수동 = `npm run smoke:layout`).
`index.html`을 스테이징하면 pre-commit이 자동 실행하고, 아래 계약이 깨지면 커밋을 막는다(fail-soft — playwright-core·chromium이 없으면 SKIP).

| # | 계약 | 허용 오차 | 정본(구현) |
|---|---|---|---|
| ① | 좌·우 유리 블러 박스(`[data-bizmbox]`) **상단·하단선 일치** | Δ ≤ 1.2px | `_srailAlignTop` 이젤 — 하단선 = 뷰포트 − 점줄 블록 − 24, 높이는 열별로 「하단선 − 자기 상단」 |
| ② | 그 안 **마지막 흰 도형** 하단선이 좌·우 같은 수평선 | Δ ≤ 1.2px | 우 목록 스트레치(부족분 가산) · `data-bizmfill` fill 패스 · 마지막 도형 여백 0 규약 |
| ③ | 흰 도형 하단 ↔ **박스 안선**(패딩 18) 밀착 = 넘침도 빈 유리도 없음 | \|틈\| ≤ 2.5px | `_bizFitViewport`(박스 내부 넘침 기준) · `_bizFitSeason` |
| ④ | 재계산(`_srailAlignTop`·fit) 12회 반복해도 **같은 값** | 상태 1개 | 위 계산식이 전부 비순환·멱등 |

검사 면 = 1면(판매 실적 데이터 有/無) · 3면(짝 면) · 4면 · 1면 복귀. 데이터는 `tools/qa_mock_ops.mjs`의 **결정적 목데이터**(시드 고정)로 `?qa=admin` 경로에 주입 — 실API·실데이터·PII 미접촉.
좁은 화면(2열 하한 미만 = 이젤 비적용)은 계약 대상이 아니라 자동 제외(모바일 자연 높이 회귀 0).

### 4.2 2열 하한(대시보드 판형 임계) — 값은 한 곳, 짝은 게이트가 지킨다 (260807-10 신설)

「좌·우 두 열로 펼치느냐(2열) / 위아래로 쌓느냐(1열)」를 정하는 **폭 하나**가 대시보드 전체를 가른다.
이 값은 **두 언어에 나뉘어** 있어 손으로 맞추면 반드시 갈린다 — 그래서 정본을 못 박는다.

| 자리 | 정본 | 비고 |
|---|---|---|
| JS 수 | `var _BIZ_WIDE_MIN=1365;` (index.html 첫 페인트 전 블록) | **여기 한 곳** — `_bizZoomTarget`이 본문 스크립트보다 먼저 실행돼서 이 블록에 둔다 |
| JS 판정 | `function _bizBookWide(){ return window.innerWidth>=_BIZ_WIDE_MIN; }` | 폭 비교는 앱 전체가 **이 함수만** 부른다(구 `innerWidth>1200` 산재 8곳 청산) |
| CSS 짝 | `@media(max-width:1364px){#sales-rail{display:none}}` | JS 하한의 **여집합**(N−1). 어긋나면 「레일만 뜬 반쪽 레이아웃」 |
| 게이트 | `tools/check_wide_threshold.py`(하드 0 · pre-commit) | ①상수 1곳 ②판정식이 상수를 그대로 씀 ③CSS 짝 = N−1(**줄머리 앵커**) ④`innerWidth`↔1100~1600 리터럴 비교 0(별칭 `w.innerWidth`도 잡되 주석 안 예시는 건너뜀) |

**값의 근거 = 실측**(무패치 폭 스윕 `tools/scratch/probe_wide_threshold.mjs`): 2열 반쪽 칸이 「판매현황 상세」 표
바닥폭(612.5px)보다 좁으면 맨 오른쪽 매출 열이 카드 밖으로 밀려 **통째로 사라진다**(가로 스크롤바도 `height:0`이라 신호 0).
넘침 = 1201:82px · 1280:43 · 1340:13 · 1360:3 · 1364:1 · **1365:0** → 하한 1365(운영자 승인 260807-10 「응 다 배선 ㄱㄱ」).
⚠ **표 폭이 바뀌면 이 수도 다시 재야 한다** — 260807 리포트 1판의 「1281」은 그 뒤 열 간격 확대(`_colGap` 12→22 · 5열)로
표가 42px 넓어지기 전 수치였다. 열을 더하거나 패딩을 키운 세션은 위 프로브를 돌려 하한을 갱신할 것.
⚠ `.pf`/`.ps`(헤더 사용자명 전체↔약칭)의 `max-width:1200px`는 **다른 축**(헤더 혼잡도)이라 같이 안 옮긴다.

## 5. baseline 이력

| 날짜 | index hex | signage hex | 사유 |
|---|---|---|---|
| 260703 (패치 원본) | 1797 | 2 | nomute-editor 검증본 기준 실측 |
| 260703 (적용 시 재실측) | **1827** | 2 | PR#80 로딩 성능(798da91)+배치 위저드 시리즈(febb8d9~3e09e94) +30 |
| 260704 | **1825** | 2 | PR#93 이메일 재설정 -1 + 홍보 지도 대시보드(빈상태 `#888` 제거) -1 — 청산 하향 |
| 260704 (v4) | **1822** | 2 | 홍보 지도 v4 — 플랫폼 트리 인라인 `#eee`·`#fafafa` 토큰화 -2 + 사이니지 관리 버튼 제거(`#fff`) -1 |
| 260704 (PWA) | **1823** | 2 | PWA 설치 지원(운영자 요청) — `<meta name="theme-color">` +1 (HTML 스펙상 리터럴 필수, var() 불가) |
| 260704 (rebase) | **1824** | 2 | PR#96(이메일 후속, 구 baseline 기준 통과분) 이월 — `#aaa` +1 |
| 260704 (레드 확립) | **1758** | 2 | 유일 레드 확립(운영자 확정) — `--danger`·`--danger-btn` 토큰 편입 + raw 62곳 `var()` 치환(-66, 차트 리터럴 8 의도 잔존) |
| 260705 (팔레트 통폐합) | **1745** | 2 | 9개 토큰 `var()` 별칭화 -13, 고아 -1 — `danger→danger-btn·kakao→c6·blog→green·etc→c5·past-text→muted·surface→glass-surface·glass-border/glass-bd→glass·nm-bg→past-bg·off-bg→border2`. "여기서 색 더 안 만듦" 팔레트 폐쇄 확정(운영자) |
| 260705 (앱아이콘 등록) | **1745**(무변) | 2 | YM 캘린더 앱아이콘 세트 등록(운영자 확정 "ㄱㄱ 굿") — **index hex 무관**(baked SVG/PNG 에셋 = `var()` 불가, §🚨 승인 리터럴). 아이콘 리터럴 = 종이 `#5E61EE·#4A4DE7·#3F42DC`(`--accent` 명도 변주)·뒷장 `#2E31A8·#252892·#3B3EC6·#3134B4·#4A4EDC·#585CE8`(코발트 저명도, 겹침 대비)·살몬 띠 `#E29A6E·#D88455`(`--peach-text` 명도 변주)·실버 링 `#F0F0F4·#B9B9C4·#8A8A96·#DADAE2`(중립)·보드 배경 `#FBF0EC`(=`--peach-light` 토큰값). 소스 = `image/app-icon.svg`, 파생 = favicon(svg/16/32)·pwa 192/512/512m/192m·apple-touch-180·og 1200×630. `<head>` `rel=icon`·OG 메타 + `manifest.webmanifest` 등록 |

| 260705 (앱 글래스 통일) | **1661** | 2 | 앱 전체 칩/버튼을 `.pm-fbtn` 글래스 톤(`var(--glass-surface)`+`backdrop-filter:blur(8px)`+`var(--glass-bd)`+`var(--glass-shadow)`+`var(--text)`)으로 통일(운영자 "다 앱내에도 통일") — raw hex -84. 명명 16종(sp-chip·st-chip·nb-chip×2·ana-chip/pchip/back·ana-xbtn·sub-pop-copy·cb-chip·todo-impbtn·prog-tab.active·fbar-seg/chip·sp-time-\*·adm-btn·m-btn.cancel) + 인라인(닫기원형·모달푸터·페이지네이션·뷰모드·사이니지). CTA(`.btn--primary/--danger`·`.m-btn.save/del`)·모달/카드/팝오버/챗버블·select(ana-sel 등)는 제외(non-glass 유지). 분신술 5인 검증(계약·회귀·시각·성능 APPROVE, 완결성 결손 수정 후 재검증) |
| 260710 (전앱 스크롤바 + 보드 스택 청산) | **1635** | 2 | **전앱 커스텀 스크롤바 신설(운영자 확정)**: 트랙 = 투명 + 보더 `--c2` alpha 50% 변주(rgba 216,132,85,.5 — §3.5② 정상) · 썸/상하 삼각 버튼 = `var(--c2)` — 신규 색 0, 웹킷 전용(Firefox 기본 유지: standard 속성 병기 시 크로미움 웹킷 스타일이 꺼져 의도적 미병기). 기존 국소 규칙(캘린더 `.c-inner` 얇은 회색·`.chips` 숨김)은 셀렉터 구체성으로 유지. + 분석 보드 카테고리 탭 폐지(공연·전시·교육 스택)로 참조 0 된 `.ana-tabs/.ana-tab` 규칙·구 전시 로더 청산(중간 단계 포함 -6, 세부 = check_design.py 주석) |
| 260711 (스크롤바 오버레이 통일 · 구획 폐지) | **1631**(무변) | 2 | **↑ 260710 스크롤바 설계(보더 트랙+살몬 ▲▼ 화살표) 대체(supersede)**: 전역 `::-webkit-scrollbar`를 구획 없는 오버레이로 통일(운영자 "칸 따로 떼지 마라 — 콘텐츠 100% 꽉·스크롤 없는 셈 치고 위에"). ① 컨테이너(모달·패널·팝오버) 세로 = `width:0`(콘텐츠 edge-to-edge) + `:hover` 8px 리빌 = PR 246 판매레일 미러 · ② 루트(html, 문서 스크롤 앱) 세로 = 상시 8px `html::-webkit-scrollbar`(뷰포트 진입/이탈 리플로우 방지 — 웹킷 클래식은 edge-to-edge·리플로우0·가시 동시 불가 → 루트는 리플로우0+가시 택, edge-to-edge 포기) · ③ 가로 = 상시 8px(넓은 보드 테이블 발견성). 살몬 화살표·보더 트랙 폐지. 색 = 무채 흑알파 rest `rgba(0,0,0,.15)`(캘린더 `.c-inner`·판매레일 계승) + **active `.28` = 신규 알파스텝**(§3.5② 흑알파 변주 · 260710 `--c2 .5` 등재 전례). 신규 hex 0(1631 무변). `_sbTrack` 유지(data-sbmove 구동)·data-sbpos 미사용 잔여(후속 청산). §기틀검증 5인 2R 만장일치(루트/가로 분리로 blanket 리플로우·발견성 MED 해소 · 헤드리스는 오버레이 환경이라 무에러만 확인, 클래식 거동은 라이브 검증). |
| 260711 (상단 대메뉴 분리) | **1629** | 2 | PR#272 「상품 등록 및 홍보 신청」→「상품 등록·변경」+「홍보 신청·확인」 분리 — 신규 드롭다운 빌드 함수 색 토큰 계승, 구 `_scheduleMenuBuild`의 #333·#999 청산 -2 (check_design 주석 등재분 §5 미러) |
| 260711 (영상 편집기 3분류) | **1627** | 2 | 콘텐츠 제작 ▸ 영상 편집기 신설(운영자 260711 — 영상 편집·자막 삽입·영상 프롬프팅) — 신규 ve-* 코드 raw hex 0(전부 토큰·기존 알파 변주), 콘텐츠 제작 드롭다운 재작성 시 기존 #999·#333 → `var(--dim)`/`var(--text)` 청산 -2(메뉴 분리 -2와 별개 인스턴스). 자막 색·배경 색 선택지 = §0 컬러칩 토큰만(팔레트 폐쇄형) |
| 260803 (Q39 입력칸 클립·동기화 생명선) | **1605**(무변) | 2 | 노뮤트 에디터 기능 배선(§2 #14 신설) — 클립 CSS 전량 `var()` 토큰 + 흑알파 기존값 `rgba(0,0,0,0.04)`(L52 `--glass-shadow` 성분·`.arrow` 자구 동일) = **신규 hex 0·신규 알파스텝 0**. 폴백 다이얼로그 = `.u-pop`/`.u-backdrop` 값 자구 계승 + `.btn.btn--sm` 재사용. 평의회 8인 적대 검증(팬텀 클립·폴백 좌붙음·대비 2.63:1 → 전부 수정 반영) |
| 260806 (버튼 「덜 투박하게」) | **1550**(무변) | 2 | 운영자 「★ 부드럽게」 선택(시안 = `docs/reports/260806_버튼_덜투박_플레이그라운드.html`) — `--r-btn` **10px→12px** 토큰 갱신 + `.btn` 눌림/2겹 그림자/`:focus-visible` 신설/주력 커브 + `.cm-tool` 30→32(원형 사다리 정본값)·테두리·라운드 10. **raw hex 무변** = 새 색 0(그림자는 전부 기존 토큰 alpha 변주 §3.5② · 링은 `--accent-glow` 토큰 · 테두리는 `--border` 토큰). 근거 = 2026 UI 베스트 리서치(눌리는 버튼·깊이의 귀환·포커스는 티 나게) |

## 5.5 표기 규약은 **화면 단위로 갈린다** (260807 인벤토리 — 「통일」하지 말 것)

> 날짜·정렬 표기가 앱 안에서 여러 갈래인 것은 **사고가 아니라 화면마다 목적이 달라서**다.
> 다음 세션이 「어긋났네」 하고 한쪽으로 통일하면 그 화면이 풀려던 문제가 되살아난다. 아래가 실측 인벤토리다.

### 날짜 표기 6종 (전부 의도)

| 표기 | 자리 | 왜 그 표기인가 |
|---|---|---|
| `1/9` · `1/24-25` · `11/28-12/2` | **판매현황 상세 표**(`_bizDateCell`) | 운영자 260807 — 요일·선행 0을 걷어 제목 칸을 넓힌다. **정렬 = 그냥 좌측**(260807-3 운영자 「일시 그냥 좌측정렬할게」) — 한 자리 월을 `min-width:2ch` 상자로 한 칸 들여써 슬래시 세로선을 세우던 시안은 실물을 보고 **철회**했다. 전 행 왼쪽 끝이 한 줄로 서는 쪽이 낫다는 판정 |
| `01.09(금)` | 모달 `_bizRender` | 260807 개정 대상 밖(운영자 「이 화면」) — 구 표기 유지 |
| `9.3 목` | 판매 실적 표 · 레일 · 개별 사업 내역 | 카드/레일 밀도용 최단 표기 |
| `1.24(토)~25(일)` | `_salesFmtDates` 실적 조회 | 회차 나열 |
| `01.24~01.25` | 3면 좌 열 차트 축·툴팁(`_bizDrawSalesBars`) | 45° 눈금 — 자릿수가 흔들리면 라벨이 어긋나 보여 **선행 0 유지**(한 자리 월 `2.05`는 소수로 오독, 260806b 실측) |
| `1.24` · `1/9` | 전시 일자 · SVG 축 | 축 눈금 |

⚠ **3면은 좌 차트(`01.24~01.25`)와 우 표(`1/24-25`)가 같은 `list`를 다르게 적는다** — 위 사유대로 의도된 것이다.

### 열 정렬 규약

- **장르 열 = 좌측 정렬**이 나머지 3표(`_railYrmPerfTable` · `_srailTableHtml` · 모달 `_bizRender`)의 규약이다.
- **판매현황 상세 표만 우측**(운영자 260807 「장르를 우측으로 … 회차랑 거의 맞먹는 수준으로」) — 제목 칸의 오른쪽 경계를 한 줄로 세워 1행 말줄임 제목이 최대 폭을 쓰게 하려는 것. **다른 표를 여기 맞춰 우측으로 돌리지 마라.**

### 머리글 세로 정렬 2종 (둘 다 운영자 결정 — 통일하지 말 것 · 260811 신설)

머리글 행에 **부줄이 붙은 칸과 안 붙은 칸이 섞이면** 안 붙은 칸을 위에 매다느냐(`top`) 가운데 두느냐(`middle`)를 정해야 한다.
답은 **부줄 칸이 소수냐 다수냐**로 갈리고, 두 표가 정확히 반대다.

| 표 | 정본 | 부줄 비율 | 운영자 결정 |
|---|---|---|---|
| **판매현황 상세**(`_bizListTable` ~L7447) | `vertical-align:middle` | **7칸 중 4칸**이 「(단위: …)」 | 260811 「1줄일 경우에는 그냥 2줄이라고 생각했을때 높이의 중앙에 글자가 오도록」 — 부줄이 다수라 그쪽이 행 높이의 기준이 된다. 구 `top`은 1줄 칸(일시·공연명·장르)을 위에 매달아 **아래로 9.0px 빈칸**을 남겼다(실측). `middle` = 1줄 글자 중심 ↔ 2줄 잉크 중심 **Δ1px** |
| **연간 실적**(`_yrTable` ~L12755) | `vertical-align:top` | 15칸 중 **1칸**(진행 연도 「상반기 잠정」) | 260723 「단줄 연도가 중앙정렬로 떠 보이던 것 교정」 — 부줄이 예외라 나머지 연도가 **한 줄로 서는** 쪽이 맞다 |

- 실측 도구 = `tools/scratch/probe_saletbl_hdvalign.mjs`(머리글 줄 좌표 + 크롭 촬영).
- ⚠ **패리티 게이트(`smoke_component_parity.mjs`)는 이 축을 못 본다** — ⓐ 두 표의 머리글 `<td>`는 **클래스가 없다**(인라인 조립)라 「같은 클래스」 키가 안 잡히고 ⓑ 측정 신호가 색·테두리·라운드·활자·패딩·그림자라 `vertical-align`이 **아예 없다**. 이 축을 지키는 건 위 표와 코드 주석뿐이다.
- 두 화면(3면 우 열 · 「사업 결과 비교」 모달)이 같이 바뀌는 건 `check_biz_list.py`(머리글 배열 1곳)가 잠근다.

---

| 260807-8 (사업 목록 표 정본화) | **1548** | 2 | 운영자 「사업 목록 리스트는 항상 저렇게 표기되게 고정 · 서로 참조값 가지도록」 — 모달 `_bizRender`가 따로 타이핑하던 사업 목록 표를 3면 우 열과 **같은 빌더**(`_bizListTable`)로 통합. 그 표에만 남아 있던 raw hex 잉크(테두리·줄무늬·글자)가 토큰 조합으로 대체 = **−2 청산**(신규 색 0). 집계도 `_bizListBuild` 한 곳으로 — 종전 「한쪽 수정 시 반드시 동시 수정」 손복사 경고 해소 |

## 6. 알려진 부채 (동결 — 청산은 운영자 판단 대기, 시각 검증 필수 별도 작업)

1. `--kakao` 이중 정의: 블록1 260705 `var(--c6)` 별칭화로 **값 일치**(부채 완화 — 시각 불일치 해소). 완전 청산(블록1 정의 삭제)은 대기. JS 하드코딩 `#C8900A` 2곳(~L1640·~L12858) 잔존
2. ~~위험 빨강 2종~~ → **✅ 260704 확립 → 260705 1종 통일**: `--danger`를 `var(--danger-btn)` 별칭화 = 빨강 `#E24B4A` 하나(대표 `--danger-btn`). 잔존 리터럴 = 차트 내부 8곳(var() 불가, §0④) + hover `#C73F3E` 2곳 + 취소사유 모달 개별 buttonColor(`#EF4444` 등, 별도 부채)
3. 성공 초록: `--green:#1A6B3C` 있는데 raw `#0F8B4C` 32회 (260705 `--blog`도 `--green` 별칭으로 통합)
4. 고아 토큰 11개(260705 별칭화 후 -1): `--blog --border2 --c1 --etc --insta --kakao --surface --youtube --z-confirm --z-nav --z-sticky --z-toast` 계열 — 별칭화된 `--surface/--blog/--etc/--kakao`는 정의가 `var()`이나 자체 사용처 0이라 고아 유지(대표만 실사용). `--z-*` 4개는 raw `z-index` 정리와 묶어서
5. 그레이 축: raw `#888`(224) `#999`(66) `#aaa`(83) `#bbb`(68) `#666`(77) → `--dim`/`--muted` 수렴 여부 (대공사)
6. c축 밖 차트색(§0③ 참조): 의미색 맵 `_BIZ_GENRE_COLORS`(`#0891B2 #D97706 #DB2777 #9CA3AF` 등)·`_BIZ_TYPE_COLORS` + 임의 시리즈 pal 끝자락 `#7C3AED`(3곳) — 의미색 맵은 "c축 안 씀"이 코드 정본이라 **병합 대상 아님**, 토큰 편입 여부만 운영자 판단.
   ⚠️ 260805 운영자 지시로 **뮤지컬 = 구 `#2563EB`(파랑) → `--green #1A6B3C`(=`--c3`) 팔레트 칩**으로 교체(「클래식 `--accent`와 인접 blue 쌍이라 비교가 안 된다»). 이 맵의 항목이 c축 칩을 쓰는 첫 사례 = **팔레트 안 이동**(새 색 반입 0 · raw hex 총량 무변) — §0③의 "의미색 축은 c축을 쓰지 않는 게 정본"은 **나머지 항목에 대해** 유효

7. **원형 1글자 배지 어휘 이원화**(260807 짚음 · 260807-3 배색 단일화): 캘린더 셀·배치 표의 `_batchBadge`/`perfBadgeLb`는 **`대` = 대관**(`--c5` 시안블루) + 인디고 채움 원 = 기획인데, 판매현황 상세 표의 새 배지는 **`대` = 대극장**(`--accent` 인디고)다. 두 배지는 **한 화면에 공존하지 않아**(캘린더/날짜 패널 vs 대시보드 3면) §3.5③ 「인접 카테고리」 직접 위반은 아니고, title·aria-label로 뜻을 못 박아 뒀다. 어휘 통합 여부 = 운영자 판단. ⚠ 260807-3에 대·소 배색을 **한 벌**(`--accent-light` 채움 + `--accent` 글자 · 4.96:1)로 정리했다(운영자 「소극장도 같은 음영 동그라미」) — 구별은 글자가 지고 색은 「극장 표식」 한 가지만 말한다. 그래서 캘린더 인디고 채움 원(=기획)과의 형태 충돌도 함께 줄었다. **배지를 2곳째로 쓰게 되면 그때 §2 정본 승격 + 어휘 정리**(현재는 사용처 1곳 · 새 클래스·토큰·hex 0이라 §3 정문이 열리지 않는다).
8. **3면 KPI ↔ 상세 표 색축 교차**(260807 짚음): 좌 열 KPI는 평균 점유율 = `--peach-text` · 누적 판매 고객 = `--c5`인데, 우 열 표는 점유율 = `--accent` · 유료 관객 = `--peach-text`다. 같은 화면에서 같은 지표가 다른 색을 쓴다. 어느 쪽으로 맞출지 = 운영자 판단(둘 다 팔레트 안이라 게이트는 안 잡는다).
9. **판매현황 상세 표 머리글 활자**(260807 짚음 · **선재 부채**): 이 표 머리글만 12px/**400**/`--dim`이라, 운영자 260729가 표 머리글을 `--neutral-text`로 올린 결정(`.mv-tbl` ~L2436, 11.5px/700) 이전 상태다. 같은 슬롯이 슬라이드로 갈아끼우는 형제 표(판매 실적)와 머리글 결이 다르다. 되돌리면 화면이 눈에 띄게 바뀌므로 운영자 확인 후.
   ⚠ **260807-10 부분 해소** — 본문 쪽은 운영자 승인으로 올렸다: 오픈석 열 잉크 `--dim`(3.54:1 · 줄무늬 3.40 · AA 미달) → **`--neutral-text`**(5.23:1 · 5.01 · AA 통과). 이제 **본문이 머리글보다 진하다** = 위계는 정상이 됐고, 남은 부채는 **머리글·단위줄 쪽 하나**다(§6-5 그레이 축과 같이 묶어 청산).
