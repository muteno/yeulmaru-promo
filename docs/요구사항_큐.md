# 요구사항 큐 — yeulmaru-promo (지시 원장 · append-only)

> 운영자 지시를 접수 순서대로 기록하는 원장(SSOT). 완료 보고 상세는 `docs/작업이력.md`.
> 양식: `[Q. NN]` 지시 원문(+수신 KST) → 처리 요지 → 상태(🔵 진행 · ✅ 완료).
> CLAUDE.md [6] 완료 보고의 `[Q. NN]`이 이 파일 번호와 1:1.

## [Q. 01] 2026-07-17 — 앱 개발 쳇바퀴(자기 트래시 생성·재발견 루프) 진단 + "트래시 다 없애"
> "개판이네 앱이 뭘 계속 고치는데 햄스터가 동글동글 쳇바퀴만 도는 느낌이네 … (실제로 자기 자신이 쓰레기 버리고 자기 자신이 쓰레기 줍는중)(문제를 스스로 해결하려다 문제를 만들고, 다음에 그 문제가 생겼다고 또 발견함) … 3시간 뒤 … 처음에 문제를 제기한거는 정작 해결안됬음. 토큰은 다 소요"
> → 분기(AskUserQuestion "쳇바퀴 끊는 첫 수"): **"일단 트래시 다 없애"**

- ✅ 처리(완료): 쳇바퀴 엔진 4개 진단(회귀발생기·가짜완료·안닫히는트래시·메타잠식) → 실제 청소 + 회귀 안전망까지. 상세 = `docs/작업이력.md` 260717 블록.
  - 유휴잠금 지뢰 `_idleLockDisabled` true→false(44일 방치 보안구멍 닫음)
  - 로그인 흐름 디버그 콘솔 떨거지 제거(PIN·pw·email 콘솔 유출 포함, 안전 검증분만)
  - 원장 "Cloudflare 재배포 필요" 10여건 → 살아있는 목록 1건으로 합침(운영자 배포 1회 해소)
  - 살아있는 목록 완료분 취소선 처리 + check_refs 오탐 수복(이 파일 신설로 참조 정합)
  - **2차("다 지우셈")**: 死코드 155줄 삭제(`_promptSuperSecret`·nb 코멘트/톤 死클러스터·`_nbAssembleDraft`) + F5 IIFE 콘솔 유출(pw·email) 제거 — grep 호출부 0 검증 + 헤드리스 로그인 렌더·회귀 0 실측(ef9a10e). ※ 원장이 "死"라던 `_msalLoginEmail`은 실제 3곳 호출 = live라 보존(grep이 원장 오류 적발).
  - **3차 한 수("ㅇㅇ")**: 로그인 회귀 스모크 게이트 영구화(`tools/smoke_login.mjs` → pre-commit, index.html 변경 시만, fail-soft) = 엔진①(회귀) 상시 차단(c9c00f7).

## [Q. 02] 2026-07-18 — 애니어그램 → 노뮤트 에디터 이식 + 추후 계획 3만자 보고 + 페이블5 검증
> "지금 페이블 5인 작업 바로 들어가서 7분안에 예울마루 프로모에 있는 애니어그램을 노뮤트 에디터로 이식시키셈. 그리고 뭘 반영해야할지 알아내. 추후 계획에 대해 3만자로 보고해서 제일 효율적인지 다른 페이블5인이 6분안에 검증 지금 바로 시작"

- ✅ 처리(완료): `playground/enneagram` 15파일 → nomute-editor `viewer/enneagram/` 통폴더 이식(T0+4분52초 · 게이트 31체크 rc=0 · PR #2502 main 머지+read-back). 반영 항목 전수(P0~P3 12건) + 추후 계획 43,010자 보고 = `docs/reports/20260718_171207_애니어그램_노뮤트이식_추후계획_v1.md`(+동명 .html). 페이블5 검증단 5인·6분 캡 판정 = 채팅 최종 보고 ④ 참조. 운영자 결정 대기 2건 = P0-3(메인 뷰어 진입 링크 위치) · P2-1(정본 이관 선언).

- [Q. 02 추기 · 17:26] 검증단 5인 만장일치 '보완필요' → **v2 개정 = 정본** `docs/reports/20260718_172604_애니어그램_노뮤트이식_추후계획_v2.md`(+.html). 핵심 정정: 예울마루 playground = 라이브 서빙 중(HTTP 200 반증 — 이중 라이브 = 드리프트 현재형) · 정본 진입 = apps.nomute.kr/enneagram/(301+Access 게이트) · XSS 4개소 = P1-1 하드 선행조건. 결정 대기 3건(D-1 진입 링크 · D-2 정본 이관 · D-3 데이터 동기화 수위) = 채팅 일괄 질의.

- [Q. 02 추기2 · 기틀정비] "ㅇㅇ 그렇게 해줘 기틀정비 ㄱ 바로" + "시간 5분 제한 · 다수 페이블 가능" → 노뮤트 원장 Q번호 경합 소멸 규칙 기틀정비 완료: 큐 헤더 규칙 6(커밋 직전 최대+1 확정·착수 중 Q?? 스텁) + check_refs 스텁 차단 게이트(자가시험 실증) + CLAUDE.md [6] 동조·[15][16] 에니어그램 등재(P0-2ⓑ 종결) = nomute-editor PR #2514 머지(main 1df5309)+read-back. 노뮤트 원장 기재 = Q161. 평의회 = 페이블 8인 5분 캡(운영자 지시) — 판정은 채팅 보고.

- [Q. 02 추기3 · 재배치] "노뮤트에디터에 넣을 필요는 없구, 사주에 넣어주면 될듯. 지금 에디터에서 쓰는곳은 없지?" → 사용처 0 실측(코드 참조 전무·진입 링크 미부설) 후 재배치 완결: 사주 이식 = muteno/Saju app/public/enneagram(PR #66 머지 c57a7b7 · 라이브 saju02-7n8.pages.dev/enneagram/) · 노뮤트 제거 = PR #2549 머지(c08b945 · CLAUDE.md [15][16] 원복 · 규칙 6/게이트는 유지) · 원본 스냅샷 = 이 레포 playground/enneagram 그대로. 정본 축 = 사주. v2 보고서(20260718_172604) 후속 계획은 수용처를 사주로 치환 해석 — D-2(정본 이관)는 이 지시로 사실상 해소.

## [Q. 03] 2026-07-21 — 다회차 공연 단일회차 취급 재발(달 샤베트 615/926) — "전체 회차×가용석으로 나눠야"
> "이 공연 다수 회차인데 단일회차 취급되서 누적된 인원이 나열되는데. 관련해서 이전에 수정한 내역이 있어. 계속 수정 요청하는데도 이러네. 확인 한번 해줘. 전체 회차*가용석 으로 현재 예약된 내용을 나눠야해"

- ✅ 처리(완료): 재발 원인 = PR 311·313·315가 참조하는 회차 소스(총회차·회차상세·운영대장)가 미래 공연에선 전부 공란 → 조용히 ×1 낙하. 회차 신호 전무 시 공연 기간 일수 폴백(_salesSpanDays · 하루 1회 가정) + 마스터 조인 _uName 폴백 + 분모 3루프 정렬 + 드릴 추정 표기. 상세 = `docs/작업이력.md` 260721 블록. 정확 분모 확정 = 드릴 [회차·객석] ⚡자동 생성 2클릭(운영자).

## [Q. 04] 2026-07-21 — 링크 자료수집 도구 신설(링크트리발) — "URL만 첨부하면 쭉 다운로드"
> "혹시 이런식과 동일하게 특정 링크의 PDF, 영상까지 링크주면 사진포함해서 다운로드 할 수 있는 거를 만들 수 있겠어? 프로모에 이식가능하게 한쪽에 네이버 블로그 아래에다가 유알엘만 첨부하면 쭉 다운로드나오게하고, 이거를 나중에 노뮤트 에디터에 이식하고싶은데"

- ✅ 처리(완료): 콘텐츠 제작 ▸ **링크 자료수집** 신설 — 데스크톱 드롭다운·모바일 더보기 시트의 '네이버 블로그' 바로 아래. URL 붙여넣기 → Worker `/api/linkgrab`이 페이지 파싱(링크트리 전용 __NEXT_DATA__ + 범용 스캔) → 문서·사진·영상·압축 목록 + [내려받기]/[전체 내려받기](첨부 프록시 = 탭 즉시 저장). 유튜브 등 스트리밍 영상 = 저작권·기술상 다운로드 불가 → [열기]만(화면 고지). 상세·노뮤트 이식 노트 = `docs/작업이력.md` 260721 링크 자료수집 블록.
- [추기] 인증 게이트 실측 후 프론트 교정(인증 fetch·blob 저장) — 상세 = 작업이력 260721 추기.
- [추기2 · 260721b] 갤러리 개편(운영자 상세 스펙): 1:1 썸네일 그리드·해상도/용량 2줄·종류별 블릿 섹션·선택 다운로드·받음 표시·입력 기록 토글 = 완료. 영상 yt-dlp 다운로드 = Worker에서 실행 불가(런타임 실측) → 인프라 분기 결정 대기(작업이력 추기2 참조).
- [추기3 · 260721c] 영상 저장 = GitHub Actions(yt-dlp) 파이프라인 구축 완료(운영자 승인 · 권리 동의 체크 후 요청 가능 · 같은 영상 재요청 = 변환 생략). 상세 = 작업이력 추기3.
- [추기4 · 260721d] 입력 기록 최소화(사용자별 최근 1건 · 12시간 휘발 · 기기 로컬) 반영 — 상세 = 작업이력 추기4.

## [Q. 05] 2026-07-21 — 10분 잠금 재인증 오차단·새로고침 우회 + 캘린더 완료 표시 미반영
> "10분 잠금이 있는데, 잠겼다 풀리면 계정 재 인증을 요구함. 근데 실제적으로는 인증안하고 강제새로고침하면 접속가능. 해당 문제 확인좀해줘 계정 재인증 요구가 문제인듯" + "그리고 완료했나요? 물어서 이미 지난 프로그램일때 체크했는데, 캘린더에 빗금이 반여잉안되는데, 이것도 확인좀"
> → 분기(AskUserQuestion): 잠금 수정 범위 = **B안(헛경고+우회 둘 다)** · 빗금 자리 = **완료 처리한 홍보 글줄**

- ✅ 처리(완료): 원인 3중 = F5 복원 시 `_loginEmail` 미복원(재인증 헛경고 — 잠금 해제 설계는 원래 PIN만) + 잠금 상태 미영속(F5 = 잠금 증발) + 완료 처리 재조회 스테일(syncReload가 행 개수만 검증 → 옛 상태 되그림 = 취소선 실종). index.html 6곳 수정(이메일 재수화 · 잠금 도장 `_lockedAt` · 완료 낙관 반영+스테일 가드) · 헤드리스 잠금 4케이스 실측 ALL PASS. 상세 = `docs/작업이력.md` 260721 블록.
- [추기5 · 260721e] 대용량 영상 자동 분할(1.9GB 초과 = N편·독립 재생) 구현 + 노뮤트 이식 패키지 v1 반입(docs/reports/20260721_112741_링크자료수집_노뮤트이식패키지_v1.md/.html) — 상세 = 작업이력 추기5.
- [추기6 · 260721f] 530 오류 대응(브라우저형 헤더+재시도 사다리+한글 안내) + 안내문 1줄 축소 — 상세 = 작업이력 추기6.
- [추기7 · 260721g] [저장 요청]→[저장] + 동의 미체크 클릭 시 체크박스 스크롤+링 유도 — 상세 = 작업이력 추기7.

## [Q. 06] 2026-07-23 — 메인화면 전환: 캘린더 자리 토글 ⇄ 사업실적 뷰(좌 사업현황·우 판매현황) — 시안 선행
> "캘린더 들어가면, 좌측에 사업 현황 우측에 첨부2처럼 판매현황이 가로변의 여백을 줄여서 표현되게 할 수 있어? 프로그램은 날짜 순서로 나열되면되고, 기본 개요가 나와야함. 일시 장르 예술성, 그리고 저기 예술성 뮤지컬 어린이 이런거 대신 장소가 저기에 들어가면 됨. … 좌측 사업현황(연간 누적 실적), 우측에 현재 판매중인 프로그램 판매현황) 이렇게 볼 수 있게해줄래?시안한번 보고 가자."
> + (추가) "홍보 캘린더의 부분을 대체하면 되는건데(메인화면의) 그 부분이 토글하면 다시 홍보캘린더로 오고, 다시 토글하면 사업실적으로 가게해주면돼." + "메인화면 전면 재배치라 좀 중요해서 신경좀써줘. 빠뜨린 부분이나 머지해서 누락되게 되는부분이 없는지"

- ✅ 처리(완료): ①시안 = `docs/reports/20260723_132721_메인화면전환시안_v1.html`(PR #351 머지) ②본 구현 = 운영자 확정 「항상 사업실적이 메인, 캘린더가 서브」 반영 — index.html 메인 전환 배선(진입 기본 = 사업 실적 · nav 토글 세그 · 좌 = 연간 누적 실적 인라인 · 우 = 판매현황 컴팩트 표[날짜순·장소·일시·장르·예술성]) — 상세 = `docs/작업이력.md` 260723 블록.
- [추기1 · 260723b] "항상 지금말한게 메인이야 이제부터는! 캘린더가 서브 개념임" + "지금 들어가면 캘린더가 메인으로 보임" → 본 구현 착수·머지(시안 ★프리셋 값 = 구현 스펙으로 채택 — 좌:우 59:41 계승 · 컴팩트 표 rowpy 7 · 배지 열 생략 · 스파크 100 · 상승색 --green 대표).
- [추기2 · 260723d] 운영자 [선택값 복사] 회신 = 확정 스펙 반영: 토글 = 보드 위 칩줄 좌측(13px·패딩15) · 좌우 1:1 · 바깥 20/상단 18/간격 15 · 유리 좌 .15/우 .35·blur15·라운드 20 · 우측 표(세로 10.5·글자 14·이름 13.5·서브 12·추이 150·헤더 12) · 세션 유지(sessionStorage). + "내가 요청한게 좌측 1면, 너가 꺼내온게 넘기면 2면" → 좌측 = 2페이지(1면 = 연간 실적 2012~2026 누적[openYearBoard 재사용 pfx 파라미터화] · 2면 = 운영대장 현황[벤치마크·시즌 추세 풀구성·차트 430·KPI 20]). 진입 기본 = 사업 실적 유지(선택값의 '캘린더'는 시안 초기값 — 채팅 확정 「항상 메인」 우선, ⚠보고 고지). 상세 = 작업이력 260723 추기3.
- [추기3 · 260723e] "지금 한 화면에 가득차게 캘린더가 와버리는데, 위에 준 전환 시안에 맞춰야해" → 선택값 레이아웃·유리(1:1 · 바깥 20 · 상단 18 · 간격 15 · 좌 유리 .15 · 우 .35 · blur 15 · 라운드 20)를 biz 스코프 → **두 모드 공통(전역)** 승격 — 캘린더 모드도 시안과 동일 프레임(구 260713 캘린더 유리 .31·260710 레일 .53 확정값 supersede). 헤드리스 실측: cal 모드 레일 750/1500px·유리 .15/.35·라운드 20·여백 18/15/20 ✓.
- [추기4 · 260723f · Opus] "책장 넘기면 촤르륵 효과" + "디자인 수려하게(기틀 준수)" → ① 좌 보드 페이지 전환 = 방향성 슬라이드(bizPageR/L · 감속 이징 · dir 추적 · prefers-reduced-motion 존중) ② 글래스 정본 수려화(팔레트 폐쇄·토큰/흰알파만·신규 hex 0=1607 동결): 히어로 그라디언트+상단 하이라이트 · KPI = 글래스 서피스+역할색 액센트 바(1·2면 통일) · 차트/표 = 흰 서피스 카드 통일 · 페이지 컨트롤 = ‹ • • › 글래스 미니(하단 중앙) · 헤더 유리 입체감 · 페이지 알약. _yrRender 공유라 연간실적 모달도 동일 수려화(일관). 상세 = 작업이력 260723f.
- [추기5 · 260723h] "한수 진행 + 전시 일일 실적 입력을 못한다(공연만) — 모든 오픈된 프로그램 가능하게" → ①전시 입력 소스 확장: 전시마스터 전용이던 목록에 **오픈된 프로그램-시트 전시(t='e')** 합류(레일과 동일 오픈 판정 · 미등재분 프로그램ID로 전시일일 append · graceful 저장) = 오픈된 전시 무조건 입력 가능 ②한 수 = 성격 pill 클릭 필터(예술성/사업성 축 · 활성 링 · 전체보기 해제). 신규 색 0(1607) · 헤드리스 ALL PASS. 상세 = 작업이력 260723h.

## [Q. 07] 2026-07-25 — 진행 중인 프로그램 4건(음영 톤·호버 세부내역·네모 합침·마우스 뒤틀림) + 사업 성격 갱신 경로 질의
> "이 음영되서 자동 선택 돌아가는 부분 위에 있는 옅은 색을 기준으로 해줘" + "호버링 하면 메뉴 호버링 하는 그 로직 참고해가지고, 그 세부내역이 이 위에 호버링 되서 자연스럽게 글래스모피즘 결 따르되, 잘보이게 이 위에 뜨게해줘. 그 창에서 마우스 이탈하면 다시 없어지도록하고" + "지금 첨부2부분이 분리되어있는데 분리 안되게해주셈" + "예술성 이런거 아예 다른데 내가 모달에서 갱신해야되는거지? 엑셀로 갱신하면 뭐가 깨진다고알고있는데 … 프로그램에서 이를 갱신하고 db에 반영시킬수있는 라인이 없거든? 확인좀 해줘" + "마우스가 이 웹앱안에서 위치하는 경로에 따라 화면이 들쭉날쭉 뒤틀리는 거 처럼 보여. 뭔가 비율 기준점이 안맞는거같은데 확인좀"

- ✅ 처리(완료 · UI 4건): 자동선택 pill 음영 = 행 호버 톤(rgba(74,77,231,0.045)) 계승 · 행 호버 세부내역 팝오버 신설(nav 호버 드롭다운 문법 100% 계승 · 통과형) · 분야 네모 보더·간격 폐지(한 통) · 뒤틀림 원인 2건 제거(`.cell:hover` scale 폐지[실측] · 스크롤바 리빌을 폭→색으로 전환[코드 근거 · 헤드리스 재현 불가]). 상세 = `docs/작업이력.md` 260725e.
- ✅ 처리(완료 · 한 수 배포 260725f): **사업 성격 그 자리 갱신** — 「사업 성격」 옆 연필(그 행 호버 시 노출 · admin · 마스터 등재분)로 예술성↔사업성 토글 → `_opsMasterPatch` 로 공연마스터 '수익성' 열 반영 · 되돌리기 = 연필 한 번 더 · 실패 시 시트·화면 원상. 잔여 = 전시 성격(별 축) · 마스터 미등재분(append 경로) · 단일 셀 PATCH 승격(Worker = 평의회 대상). 아래는 착수 전 실측 답변 원문.
- 📎 실측(질의 답변): **사업 성격(예술성/사업성) 갱신 경로**. 실측 = ①값 소스는 '프로그램' 시트가 아니라 **운영_공연마스터 '수익성' 열**(전시 = 운영_전시마스터 '수익성') · 그 값이 없으면 **운영_수익성 override 시트**(`_profitLoad`) · 그것도 없으면 이름·장르 휴리스틱(`_profitType`)이 자동 판정. ②프로그램 관리 모달(`openProgramForm`)엔 **수익성 입력칸이 아예 없다**(폼 = NO·콘텐츠구분·풀네임·줄임말·시작/종료일·홍보·판매시작/종료·담당자·장소·URL = 시트 A~M). → 모달로 갱신하는 경로는 **현재 없음**(운영자 기억과 반대). ③다만 DB 반영 **라인은 있다** = `_opsMasterPatch(시트, 키열, 패치)`(마스터 재조회 → 해당 행의 그 열만 교체 → `POST /api/ops` 전체 교체)를 일일 실적 저장이 회차(공연마스터)·전시 실적(전시마스터)에 이미 쓰고 있어, **'수익성' 열도 같은 라인으로 배선 가능**(빠진 건 UI + 호출부뿐). ④"엑셀로 갱신하면 깨진다" = 축이 반대다 — 깨질 위험은 **앱 저장이 엑셀 수동관리 열을 되쓸 때**(프로그램 시트 N열=구분(장르)·O~Q=공동기획/지원사업/GS는 운영자 엑셀 수동관리라 saveProgram이 A~M만 써서 물리적 무접촉 보존 중 · 작업이력 260724 추기11~12). **수익성은 마스터 시트라 엑셀 직접 수정이 정본 경로이고 앱이 깨뜨리지 않는다.** 배선 원하면 지시 필요(모달에 성격 select 추가 + `_opsMasterPatch('공연마스터','ID',{...})` 호출 = B4 신규 요소라 착수 전 확인).

## [Q. 08] 2026-07-26 — 애니어그램 발췌 지식집 + 페이블 웹리서치 보강 (지식 추출 3부작 中 프로모 몫)
> "애니어그램 관련 발췌 지식집만 따로 빼서 나한테 줘볼래? … 부족한 부분이 없는지 페이블 1인은 검색해서 이를 더 포괄적으로 자료를 찾아오자. 특정 웹사이트들을 최대한 스크랩해서 파일로 만들어도돼. 용량은 많이" (21:12 KST 수신)

- ✅ 처리(완료): ① 툴킷 4원천(교재·analyzer·REVIEW·meta) 지식 전수 발췌 = `docs/reports/20260726_211242_애니어그램_발췌지식집_v1.md` ② 페이블 1인 웹 수집 대용량(138KB·74 URL·실패 소스 「미확인」 로그) = `docs/reports/20260726_211242_애니어그램_웹리서치_보강_v1.md`. YETA 몫(페르소나 인사이트) = YETA Q.90. 상세 = `docs/작업이력.md` 260726c.

## [Q. 09] 2026-07-29 — 진행 중인 프로그램 판매율 99%·100% = 단일회차 취급 재발 — "실제 진행회차 고려, 회차×가용석이 모수"
> "지금 진행중인 프로그램 현황 (판매 현황 부분)에 99%, 100%가 있는데 이게 사실 지금 단일 회차라고 고려해서 저렇게 나오는거야. 실제 진행회차를 고려해서 나오게 해줄래? 그동안 수정 여러번 했는데 반여잉 안되네. 변경하고, 실측해서 이미지로 전후까지 띄워줘. 각 회차*가용석 이 모수가 되야됨. 일단 기본은 그래.(회차별로 가용석이 다른거는 그 다음 문제)"

- ✅ 처리(완료): 재발 원인 = 종전 회차 폴백(Q.03 3차까지)이 전부 **공연마스터 등재분(`m`)에만** 걸려 있어, 마스터·운영대장 둘 다 없는 신규 오픈 공연은 회차가 무조건 1로 낙하 → 기준석 926이 그대로 분모. `_salesBuild` 3개 루프에서 `m` 게이트 제거 + 공연기간 확정을 분모 계산 앞으로 이동 + ×N 보정 게이트 `_rcK≥2`→`_rc≥2`. 헤드리스 전/후 실측(그날들 99%→33% · 달 샤베트 100%→34%) + 분모 7경로 회귀 ALL PASS. 상세 = `docs/작업이력.md` 260729 블록.

## [Q. 10] 2026-07-29 — 프로그램 관리에 「몇일 몇회차」 설정 + 예술성/사업성 지정 자리 부재
> "프로그램 관리 > 각 개별 프로그램 몇일 몇회차 공연인지 설정 가능해야함. 몇일은 > 기간으로 종속되면 됨." + "그리고 추가로 예술성, 사업성을 결정할 수 있는 곳이 없어. 확인 좀"

- 📎 실측(성격 지정 자리 부재 확인 = 운영자 지적이 맞음): 종전에 예술성/사업성을 **앱에서 고칠 수 있는 유일한 자리**는 판매현황 「진행 중인 프로그램」 표의 행 호버 연필(`_mvSpecPill`→`_specToggle`)뿐 — 게이트 3중(①admin ②**공연마스터 ID 등재분만** ③행에 마우스를 올려야 보임)이라 마스터 미등재 프로그램은 지정 방법이 아예 없었다. 프로그램 관리 폼(`openProgramForm`)엔 성격 칸이 없었다(Q.07 실측 답변에서 이미 확인 · 그때는 배선 지시가 없어 연필만 붙였다).
- ✅ 처리(완료): 프로그램 관리 폼에 **「사업 성격」 select**(미지정/예술성=공공성/사업성=상업성) + **「공연 회차」 입력** + **「기간(일수)」 자동 표시**(시작~종료 종속 = 입력칸 없음 · 날짜 바꾸면 즉시 갱신) 신설. 저장 = 홍보노출과 같은 부가열 단일셀 라인(`sideCells` → R열 이후 · N~Q 운영자 수동열 무접촉 불변식 유지). 판매현황 반영 = 회차는 명시 신호로 `max()`에 합류(분모 = 가용석×회차), 성격은 **프로그램 관리 지정값 > 공연마스터 '수익성' > 이름·장르 추정** 순. 목록 표에 「성격」·「일·회」 컬럼 추가. 상세 = `docs/작업이력.md` 260729b 블록.

## [Q. 11] 2026-07-29 — 전시 집계 1일차·0건에도 전면 생성 — 미상(예상 불가) = 빈 동그라미(기존 예측 표기법 계승)
> "전시 같은 경우에 지금 집계가 안생기는데 이거 1일차도 그냥 일단 집계 다 생기게해주셈 예상이 안되는 부분은 그냥 다 빈 동그라미 같은거로만 표기 기존에 예상이 안될때의 차트 표기법 등을 활용. 일단 집계는 다 되게해줘"

- ✅ 처리(완료): 전시 실측 0~1건에서 집계 표시를 막던 4곳 전부 「집계 골격」 렌더로 전환 — ① 레일 카드 스파크 `_srailSpark`(구 「추이 수집 전」 텍스트) ② 전시 「집계 전」 카드 `_srailCardEx`(구 음영 오버레이 → 실적 카드 동형 골격, 미상 수치 '—') ③ 분석 보드 스파크 `_anaSpark`(구 「데이터 부족」) ④ 전시 드릴 추이 `_anaExhibTrend`(구 「추이 데이터 부족」) + 진행중 표 noData 행 = 빈 게이지 트랙. 미상 표기 = 공연 카드 `_srailTrend` 예측점 노테이션 100% 계승(속빈 원 `--surface-solid`+`--muted` 테두리 · 회색 점선 3 3 · 수치 '—') — 신규 hex 0(오히려 -2 = baseline 1607→1605). 상세 = `docs/작업이력.md` 260729c 블록.


## [Q. 12] 2026-07-29 — 진행 중인 프로그램 「전시」 표 = 공연과 같은 출력방식으로(전시 전용 출력·디자인 삭제)
> "이거 전시부분도 공연과 같이 출력되는건 아니고, 공연의 출력방식과 동일하게 해주고 얘만 있는거 같은 출력이나 디자인은 없애줄래?" + (추가) "전시도 동일하게 판매율(누적으로만) 최근 7일 추이, (평균값/일일 을 기준으로), 사업성격 > 입력한대로 나오게 그대로 인덱싱도 동일하게 해서 해주"

- ✅ 처리(완료): 전시 전용 표 렌더러(`_railYrmExTable`) **삭제** → 전시 행을 공연 행 모양으로 정규화(`_ryExRow`)해서 공용 표(`_railYrmPerfTable`) 한 곳에서만 그린다. 섹션(공연 네모 / 전시 네모)은 종전대로 분리 유지. 사라진 전시 전용 출력 = 살구색 일자·살구색 게이지 바·「달성률/성격」 머리글·빈 장르열·「집계 전」·plain 「기획」 하드코딩·noData 행 클릭 불가. 상세 = `docs/작업이력.md` 260729d 블록.
- ⚠ Q.11(#440)과 겹치는 1줄 = 진행중 표의 전시 noData 행 「빈 게이지 트랙」 — 전시 전용 부품이라 이번에 폐지되고 **공연 noData 행과 같은 표기**(추이 자리 '데이터 부족'/'오픈 예정' · 판매율 '—')로 통일. 골격 상시화의 나머지 3곳(레일 카드 스파크·전시 카드·분석 보드 스파크/드릴 추이)은 무접촉 = Q.11 그대로 산다.
- [추기1 · 260729e] "판매율 왼쪽정렬 / 사업성격·예술성 가운데 / 장르 뮤지컬(어린이) → 어린이" + "전시는 달성률이 아니라 누적 관객수(숫자 누계) / 일자 ~11.1 → 11.1 요일" + "성격은 전시 무조건 예술성 / 최근 7일 추이는 오늘 기준 공연처럼" + "누적 관객도 강조색 볼드·판매율과 동일하게 좌측정렬" + "인덱싱(머리글) 6칸 모두 가운데 정렬" → 6건 전부 반영. 상세 = `docs/작업이력.md` 260729f 블록.

## [Q. 13] 2026-07-29 — 진행 중인 프로그램 표 좌우 스크롤바(호버 리빌) 투명도 50%
> "이거 좌우 스크롤 호버링 하면 보이는거 투명도 50%로 해줄래?"

- ✅ 처리(완료): 「진행 중인 프로그램」 카드 호버 시 드러나는 스크롤바 리빌 색 rgba(0,0,0,0.15) → rgba(0,0,0,0.075)(지금 보이는 정도의 50% · 흑알파 변주 = 기틀 예외 · 신규 hex 0). 헤드리스 실측 = 가로 넘침 628>599 재현 · 호버 computed 색 전/후 확인 · check_design rc=0. ⚠해석 = 「반투명으로 절반」 — 절대값 0.5(더 진하게) 의도였으면 숫자 1개 교체. 상세 = `docs/작업이력.md` 260729 [Q.13] 블록.
- [추기2 · 260729g] "표 인덱싱 열 크기 조정 가능하게 + 약간 더 불투명하게 / 판매율·프로그램·사업성격·장르 머리글 좌측정렬 / 전시 최근 7일 추이 일단 넣기(좌측 빈 건 그대로) / 전시 장르 「전시」 표기만 삭제" → 5건 반영 + ⚠신규 요소 = 머리글 칸 경계 드래그 핸들(공연·전시 동시 반영·기기 단위 기억). 상세 = `docs/작업이력.md` 260729g 블록.

## [Q. 14] 2026-07-29 — 전시일일에 내역이 쌓였는데 분석 보드·진행 중인 프로그램 어디에도 안 나타남
> "지금 이런식으로 기획전시 내역이 쭉 있는데 첨부2의 화면도 그렇고 첨부3의 화면에서도 안나타나. 확인좀 해줄래?"

- ✅ 처리(완료): 원인 = `_anaExhibBuild()`가 **전시마스터 행만** 훑는데, 마스터 미등재 전시의 입력은 설계상 프로그램ID를 전시ID로 **전시일일에만** 쌓인다 → 전시 객체 자체가 안 만들어져 네 화면 전부 공백. 빌더 말미에 **고아(전시일일 전용) 합류** 추가 = 분석 보드·레일 카드·진행중 표·드릴 동시 수복. 목표관객 미상 달성률은 0.0% 대신 '—'(`_exOccTxt`). 헤드리스로 운영자 시트 상태(마스터 0행+일일 11행) 재현해 누적 124명·어제 +14 일치 확인 + 정상 마스터 전시 회귀 0. 상세 = `docs/작업이력.md` 260729h 블록.

## [Q. 15] 2026-07-29 — 진행 중인 프로그램 개명·정렬 재조정 + 전시 장르 「시즌」·주관식 입력구 + 상단 nav 활성색 골드 + PLAY ZONE 통합 + 카드 블릿
> "용어 변경 / 진행중인 프로그램 > 프로그램 별 판매 추이 // 인덱싱 값 정렬 변경 / 사업 성격 > 중앙 정렬 / 장르 > 중앙 정렬 / 최근 7일 추이 > 좌측 정렬 // 전시는 장르 > 일단 '시즌'으로 통일 // 전시도 장르 선택하는 곳이 있나? 거기에 주관식으로 일단 입력하게 해줘"
> (추가) "그리고 최상단에 현재 노출하고 있는 페이지는 골드 레몬색(팔레트에 비슷한게 있나?) 이거로 채색을 해줄래? 해당 페이지가 선택 확정될때 메뉴 폰트가 그 색으로 변경 (기존 선택 시 볼드는 유지)"
> (추가) "그리고 play zone 일단 콘텐츠 제작으로 세부 메뉴를 다 넣어줘"
> (추가) "그리고 진행중인 프로그램에 / 사업성격 아래에 값들있지 예술성 혹은 사업성 / 이거 좌측정렬로. / 그리고 인덱싱 중에 '일자' 좌측정렬로 / 공연이나 전시나 다 마찬가지이고 진행중인 프로그램에 귀속되는거임. / 그리고 연도별 관람 수강추이, 전체 실적표, 진행중인 프로그램 이거는 블릿을 각각 달아주고, 블릿은 강조색1번으로 해줘."

- ✅ 처리(완료 · 7건): ①카드 제목 「진행 중인 프로그램」 → **「프로그램 별 판매 추이」** ②머리글 정렬 = 일자·프로그램·판매율·최근 7일 추이 **좌측** / 사업 성격·장르만 **가운데**, 본문 값은 전부 좌측(성격 pill·추이 막대 포함 — 추이는 우측정렬+padding-right:34 폐지) ③전시 장르 = **「시즌」 통일**(구 '전시' 고정 + 표기 삭제 → pill 표시 부활 · N열 「구분」은 폴백 사다리에서 의도 제외) ④**프로그램 관리 폼에 「장르」 주관식 칸 신설**(전시 포함 · 부가열 sideCells 라인 = 회차·수익성과 동일 · N~Q 운영자 수동열 무접촉) + 관리 목록에 「장르」 열 ⑤상단 nav 활성 글자색 = **골드 레몬 `--c6`**(팔레트 유일 옐로 · 볼드 800 유지 · 신규 hex 0) ⑥**PLAY ZONE 대메뉴 폐지** → 세부(도화지·에니어그램) 전부 「콘텐츠 제작」 드롭다운 「PLAY ZONE」 섹션으로 이관(구 딥링크 `#playzone`/`#play` = content로 alias) ⑦카드 제목 블릿 3곳(연도별 관람·수강 추이 · 전체 실적표 · 프로그램 별 판매 추이) = 강조색1 `--accent`.
- 검증(헤드리스 실렌더) = 열 x좌표 공연표↔전시표 6칸 전부 동일(0px) · 머리글/본문 computed text-align 실측 · 블릿 3개 rgb(74,77,231)·4×14 · 전시 장르 미지정→「시즌」/주관식→그 값 · nav 활성 computed color rgb(245,180,0)+weight 800 · 드롭다운 항목 11개(PLAY ZONE 2건 포함) · check_design rc=0. 상세 = `docs/작업이력.md` 260729i 블록.
- [추기1 · 260729i] "이게 진행중인 프로그램이면 그 안에 있는 표들은 다 인덱싱이 같으면 정렬도 같게 가야된다 / 공연하고 전시가 인덱싱하고 내용의 정렬값이 달라. 통일 시켜줄래? / 계속 그렇게 하도록 스모킹?해주고 / 단, 전시의 인덱싱 '값'(예: 누적 관객수)이 공연과 같아지면 안 됨. 틀만이야" → ①통일은 위 ② 변경으로 이미 성립(공연·전시가 `_railYrmPerfTable` 한 함수를 탄다 — 실측 열 x좌표 6칸 0px 일치·본문 정렬 6칸 동일) ②**회귀 차단 스모크 신설** `tools/smoke_rail_align.mjs` = 열별 정렬·경계만 비교하고 **머리글 라벨은 비교 안 함**(전시 '누적 관객수' 유지) · `.githooks/pre-commit` 배선(index.html 스테이징 시·fail-soft) · `npm run smoke:align` · 음성 테스트로 검출력 확인(불일치 6건 rc=1 · 라벨 오검출 0).

## [Q. 16] 2026-07-29 — 진행 중인 프로그램 행 클릭 → 개별 상세로 「촤르륵」 전환 · 호버링 폐지
> "저 대시보드에서, 각 프로그램 클릭하면 캘린더 했을때 우측에 나오는 개별 내용이 나오게 해줄래? 호버링 말고, 호버링은 그냥 없애주고 / 각각 누르면 세부 판매내역이 나오는 느낌으로다가 / 근데 첨부2 처럼 이 안에 국한해서 바뀌는거임" + "누르면 촤르륵 하면서 화면이 책넘기듯이 대체되면됨. 풀로"

- ✅ 처리(완료): 호버 팝오버(`_ryPop*`·`.ry-pop*`) 전면 폐지 → 행 클릭 시 `#rail-yrm-list`가 통째로 그 프로그램 상세로 교체(풀). 상세 카드 = 캘린더 우측 레일 카드 그대로(`_srailCard`/`_srailCardEx`), 넘김 = 좌 보드 책장 전환 그대로(`.mv-swipe` bizPageR/L 0.42s). [‹ 목록] 복귀·같은 행 재클릭 닫기·상세 중 데이터 갱신에도 유지. 부수로 「표에 보이는 행이 안 눌리는」 목록 불일치 구멍도 수복. 상세 = `docs/작업이력.md` 260729j 블록.

## [Q. 17] 2026-07-29 — 인덱싱 「사업 성격」·「장르」도 좌측 정렬
> "그리고 인덱싱 중에 사업성격, 장르 이거 좌측정렬해줘 이거는 바로 확인하고 머지"

- ✅ 처리(완료): 머리글 정렬 배열 `HA` 마지막 2칸 center → left = **6칸 전부 좌측**(본문 값은 이미 전부 좌측이라 머리글↔값 축 완전 일치). 헤드리스 computed style 실측으로 공연·전시 두 표 12칸 확인 · check_design rc=0. 상세 = `docs/작업이력.md` 260729k 블록.

## [Q. 18] 2026-07-29 — 상세 뒤로가기 = 우상단 정본 원형 버튼으로 교체(좌측 「‹ 목록」 삭제)
> "이거 목록 ‹ 말고 ‹ 버튼있찌? 첨부2는 x긴 한데 동일하게 전 단계로 가는 모양있을거야 / 그거로 우상단에 항상 디자인 기틀에 있는 마진대로 박아주고, 목록 ‹ 이거는 삭제해줘 / 삭제한만큼 간격 재조정하면됨."

- ✅ 처리(완료): 기틀 §2 정본 컴포넌트 7 **`.modal-x`**(첨부의 그 X 버튼)를 라벨만 `‹`로 바꿔 상세 우상단에 배치 — 오버라이드는 마진 0 하나뿐(sticky·float·32원형·색은 정본 상속). 자체 제작 `.ry-back` 버튼·CSS 3줄 삭제 + 상단 패딩 회수로 상세가 그만큼 위로. B2 실측 = 버튼 우변·상변 모두 리스트 안쪽 여백과 **0px** 일치. 상세 = `docs/작업이력.md` 260729l 블록.

## [Q. 19] 2026-07-29 — 뒤로가기 버튼 자리 = 모달 X와 동일 · 상세 추이 차트 뭉개짐 수복
> "그리고 내가 아까 말한대로, x의 위치가 항상 있는게 있어. 창마다 그 위치에 ‹ 랑 x 둘다 맞춰서 넣어달라는거야 / 그리고 지금 첨부1처럼 차트가 뭉개져있으니까, 그에 맞춰서 제대로 넣어줘 완전깨짐."

- ✅ 처리(완료): ①모달 X 정본 좌표 실측(창 우변 15 · 상변 7 · 32×32) → 상세 `‹`를 카드 제목줄로 이사해 같은 좌표에 배치(리스트 `overflow:hidden`에 잘리던 실제 결함도 해소) ②차트 뭉개짐 = `preserveAspectRatio="none"` + viewBox 폭(`_srailChartW`, 레일 기준)이 상세 실폭과 불일치 → 실측 폭으로 재렌더(2회 수렴)해 **가로·세로 배율 1.000** 확보. 상세 = `docs/작업이력.md` 260729m 블록.

## [Q. 20] 2026-07-29 — 병렬 검수 1인 투입(요구사항 정리 + 작업 검수)
> "오퍼스 1인 한명 돌려서 내 요구사항 정리시킨다음에 너가 한 작업 검수하게 해. 병렬로 ㄱ"

- ✅ 처리(완료): READ-ONLY 검수관 1인 병렬 투입 → [Q.12]~[Q.18] 지시를 원자 단위 45개로 재구성하고 코드·실렌더로 대조. **완전 미충족 0** · ⚠ 부분 2건(둘 다 [Q.13] 스크롤바 축 = 타 세션 영역) · 코드 결함 6건 지적. 그중 **내 작업이 만든 하자 3건(`_exOccTxt` NPE · 죽은 `.ry-pop*` CSS · 주석 불일치)만 즉시 보수**, 나머지 3건(죽은 렌더 경로 `_srailTableHtml` · 필터 해제 UI 부재 · 사문 스크롤바 규칙)은 범위가 커 **보고만 하고 운영자 판단 대기**. 상세 = `docs/작업이력.md` 260729n 블록.

## [Q. 21] 2026-07-29 — 상세 제목 간격 2.5배 + 하단 프로그램 넘김(‹ • • ›)
> "이제 프로그램 별 판매추이랑 아래 타이틀하고 간격을 지금보다 2.5배는 벌려줘야해." + "그리고 그 맨 아래에 ‹› 넘기기 버튼있찌? 그거를 저 ai 요약 아래 까지 가져와서. 넘기면 다음 프로그램이 나오게해줘. 마지막 프로그램이면 처음으로 가는거고"

- ✅ 처리(완료): ①간격 실측 6px → **15px**(정확히 2.5배 · 상세에만 적용) ②AI 요약 아래에 좌 보드 페이지 넘김 부품(`.bizm-pgctl` ‹ • • ›) 그대로 배치 — 순서는 표에 보이던 DOM 순서를 진입 직전 캡처, 마지막↔처음 **순환**, 점 직접 클릭·방향별 책장 넘김 지원. 상세 = `docs/작업이력.md` 260729o 블록.

## [Q. 22] 2026-07-29 — AI 요약을 모든 프로그램에 · 분야 시그니처 색 · 데이터 부족 문구
> "ㅇㅇ 지금에 이제 모든 구역마다 ai 요약을 붙여줄래?"
> → 범위 확인 회신: "모든 프로그램마다 붙이고, 내용을 분석할 수 없으면 「AI 요약하기에 데이터가 부족합니다」 나오게 하면 됨. 그리고 그거 에이아이 요약 공연은 시그니처 색이 들어가있는데, 전시는 전시에 맞게 시그니처색. 각 프로그램별 시그니처 색으로 들어가면됨. 이거는 각 판매현황에 쓰이는 프로그램 별 구분자도 마찬가지야" · 문구 규칙 = **기존 규칙만 계승**(선택)

- ✅ 처리(완료): 공연 전용이던 AI 예상을 **공연·전시 전 카드**에 부착 · 실측 부족 구간은 빈칸 대신 「AI 요약하기에 데이터가 부족합니다」 · 라벨 색을 **분야 시그니처 토큰**(공연 `--accent` / 전시 `--peach-text`)으로 파라미터화(판매현황 구분자 `--gtok`와 같은 축, 그쪽은 이미 분야색이라 무접촉) · 전시 예상은 공연과 같은 기울기 외삽 문법 계승(시계열 = 누계총인원, 단위만 명). 헤드리스 4케이스 실측. ⚠ 「구역」 = **프로그램 단위**로 확정 — 좌 보드·모달·분석 보드는 미부착(별도 지시 필요). 상세 = `docs/작업이력.md` 260729p 블록.

## [Q. 23] 2026-07-29 — 상세 제목 간격 1.2배 + 타이틀 영역 15% 확대
> "ㅇㅋ 타이틀 위에 프로그램 별 판매추이랑 간격을 지금이 10이라면 2정도만 더 벌리자. 그리고 타이틀 영역 더 커도됨. 첨부1의 부분 모두를 말하는거임 한 15% 더 커져도될듯"

- ✅ 처리(완료): 간격 15 → **18px**(×1.2) · 타이틀 영역(프로그램명·D-day·메타줄·점유율 행) 전부 **×1.15**(16.5→19 / 13.5→15.5 / 12.5→14.4 / 26→29.9 / 11.5→13.2) · 상세 스코프라 레일 카드는 무변(실측 대조 확인). 상세 = `docs/작업이력.md` 260729q 블록.

## [Q. 24] 2026-07-29 — 분야 머리글 표식(공연·전시) = 세로 막대 → 원형 블릿
> "이거는 내가 체크한 부분은 블릿으로 해줄래? 색상은 유지하면서" (첨부 = 판매 현황 「공연」·「전시」 머리글 앞 표식 2곳에 빨간 동그라미)

- ✅ 처리(완료): `.ry-grp-hd::before`를 4×14 세로 막대 → **8×8 원형 블릿**(값 = 정본 컴포넌트 6번 칩의 `.chip-dot` 100% 계승) · 색은 종전 `--gtok` 그대로(공연 `--accent` · 전시 `--peach-text`) · 카드 제목(`.ct-bul` 막대)은 무변이라 대분류↔중분류 위계가 모양으로 갈린다. 신규 hex·px 0. 상세 = `docs/작업이력.md` 260729r 블록.

## [Q. 25] 2026-07-29 — GS MISO 플랫폼 이식(1단계 = iframe 임베드 + 로그인 배선 제거)
> "이 프로그램을 특정 gs miso라는 플랫폼 안으로 이 깃에 잇는걸 다 집어넣으려고해. 어떤 어려움이 있을까?"
> → "일단 급하면 그 안에서 동작하는것만 보여줘도되긴해. 나중에 완벽하게 이식하고" / "로그인 배선 없애도 되거든" / "디자인이 안완벽핻됨" / "근데 데이터는 다 들어가야함"
> → 분기(AskUserQuestion 보호수준): **"0510 입력하면 무조건 가능하게 변경, 그니까 첨에 핀값 입력하는 곳부터 나오는거로"** · 폭 결정 = "미소 화면을 보면서 해야되기때문에 다른 섹션에 가서 대작업을 해야될거같아. 인수인계서 작성좀."

- 🔵 진행(인수인계 완료 · 구현은 후속 세션): 조사·실측 전량 + 설계안 = `docs/reports/20260729_MISO이식_인수인계서.md`
  - **MISO 정체 실조사**: 52g+GS네오텍 노코드 **AI 에이전트 빌더**(앱 유형 = 에이전트 앱·워크플로 앱 2종) — 임의 HTML/JS 호스팅은 공개 문서에 **언급 없음** → 「외부 URL iframe 자리가 있는지」가 다음 세션 첫 확인 항목.
  - **iframe 가능 실증**: 라이브에 `X-Frame-Options`·`frame-ancestors` **없음** · cross-origin iframe(8443 앱↔8444 포털모의) 실렌더 성공(내비 12·카드 4·JS 에러 0 · localStorage·SW 접근 가능 · 모바일가드 미발동).
  - **「디자인 안 완벽」 원인 규명**: 앱 결함 아님 = **폭 부족**. 겹침 실측 1440/1600/1920 = **0**, 1280 = 1(+가로스크롤), 1206 = 2, 1024 = 4, 900 = 5. → 1440 확보 or `transform:scale()` 축소(**앱 수정 0**).
  - **`?qa=1` 재활용 불가 판정**: `api()`가 `window._QA`면 `_qaApi`로 **실네트워크를 차단**(목데이터 전용) → 요구 "데이터 다 들어가야함"과 원천 양립 불가 → **`?embed=1` 새 진입 경로** 필요.
  - **로그인 제거가 가능한 근거**: Worker 게이트 = `X-App-Password` 단독(`roleOf` = APP/ADMIN_PASSWORD) — **MSAL 토큰은 Worker 호출에 미사용** → 로그인 걷어내도 실데이터 나옴. 진입은 기존 `_enterAppFromSession()`(pw만 있으면 로그인화면 없이 직행) 재사용 = 새 라우팅 창작 0.
  - **보안 현황(정직)**: `0510`은 이미 `index.html` 6곳·라이브 배포본 9곳에 **평문 존재**(`goToPinStep`의 `loadManagers()` 선행 호출용) → 요구 4는 보호수준을 새로 낮추는 게 아니라 현 상태의 UI 반영. 단 「주소+0510 = 사외 접근 가능」 성질은 잔존 — 운영자 인지 후 진행 결정.
  - **부작용 목록**: MSAL 제거 시 MS Graph 7종(내드라이브·sharedWithMe·followedSites·sites 등) 사망 → embed 모드에서 숨김/안내 필요 · 30분 자동잠금(`IDLE_MS`) 시연 중 잠김 검토.
  - **미확인(담당자 문의 필요)**: MISO iframe 자리 유무 · 내주는 폭 · 사내망에서 `workers.dev`·`graph.microsoft.com`·CDN(jsdelivr·plotly) 아웃바운드 허용 여부.

## [Q. 26] 2026-07-30 — MISO 이식 2단계: DB 레포 통합 + 내부망 전달 킷
> "v0 에서 역으론 못들어갈거야. 그냥 gui 구현기능을 붙였다는거거든" / "플로우로 만들던지, 아니면 에이전트로 만들던지. 거기에도 opus 가 붙어있긴한데, 이를 어떻게 전달해야 거기서 쉽게 구현이 가능할지 모르겠네"
> → "인증 이런거는 뺄거구, 깃에 달려있는 키 입력하는거 그거는 그 미소 안에 라이선싱 apikey가 회사마다 따로있거든? 그거 넣기만 하면 작동할거야" / "핵심은 유관된 db를 다 거기로 넣을건데 … 내가 db를 리포지토리에 줄테니까 하나로 합쳐줘"
> → "깃 리포지토리 최상단에 지금 db 2개 넣어놨어. 그거 회원거는 내일 다시 넣는다고 가정하고 그 구조보고 미리 설계만 해놔. 그리고 그거말고 db 정본은 그대로 녹여내면 됨."

- 🔵 진행: DB 통합 빌더(`tools/miso/build_db.mjs` + `xlsx_to_csv.py` + `data/db_export/` 수신 구조) 구축 — 레포 내 유관 데이터 4종(전시 2·교육기관 2, 1,960행) 즉시 병합 완료(`data/miso_db.json` + `data/miso_kb/*.csv`). 회원 DB = 데이터 미반입·구조 설계만(`meta.memberSchema`) 규칙 내장. 내부망 복붙용 전달물 = `docs/reports/20260730_MISO현장실행킷.md`(분기 결정트리·v0 래퍼 프롬프트·에이전트 시스템 프롬프트·워크플로 API 계약·MISO 키 치환표). ✅ 260730b: DB 2개 도착 → 정본 xlsm 24시트 병합 완료(27데이터셋 12,734행) · 회원 설계 실측(원장 반입금지, 집계 2종 반입 1순위 제안) · Opus 4.8 빌드명세(`docs/reports/20260730_MISO빌드명세.md`) 작성 · 원본 2파일 git rm(PII — 이력 잔존은 운영자 판단). ✅ 260730c: 이관본/ 단일 홈 재편 + **통짜 이식판 standalone.html**(원본 무변·PIN 0510 게이트·데이터 내장·네트워크우선/오프라인폴백, 헤드리스 스모크 통과) + 회원 반입 패키지(비공개/ 29,752행+집계3, gitignore 차단) + 전시 최근본만 확정. ⬜ 잔여 = MISO 실환경 첨부·서빙 확인(운영자) · 회원 집계 공개 번들 편입 승인 · 팀즈 의존 기능 후속. 상세 = 작업이력 260730a·b·c. ✅ 260730d: 적대적 평의회(Opus5×5+Fable1 max) 검수·반영 — 빈행오염(홍보기록 998→137 등 Worker 미러 필터)·단일파일 자산404 8종(인라인/플레이스홀더/edu주입)·연락처 마스킹·용량 5.21→3.85MB·shim 3종 보강·수신LLM 배너/지시문·격리 스모크. 정책 3건(이력PII·공개서빙·Worker쓰기노출)은 운영자 판단 분리. 상세 = 작업이력 260730d.

## [Q. 27] 2026-07-31 — 메인 판매현황 표: 「사업 성격」 열 폐지(장르가 그 자리) + 신규 프로그램 7일 추이 = 우측 1칸만
> "메인에 판매현황 나오는 부분 / 사업 성격 > 없애주고, 이 부분에 장르가 위치하게 해줄래? / 그리고 트리플빌 처럼 기존에 없었떤 내용 이 있으면 7일치 전꺼는 없는 것처럼 쭉 해준다음에 가장 우측에만 나오게하면됨."

- ✅ 처리(완료): `_railYrmPerfTable` 6칸 → 5칸(일자·프로그램·판매율/누적 관객수·최근 7일 추이·장르) · 성격 셀(`_mvSpecPill`) 제거 · 열 폭 13+11 → 장르 24 합산(총합 100 보존) · `_mvBars` 막대 칸 **7칸 고정**(없던 날 = 빈칸 → 신규 프로그램은 맨 우측 1칸만, 행 간 세로 오와열도 함께 정렬). 전후 = `docs/reports/20260731_판매현황_장르열_전후.html`(+ .png). 상세 = `docs/작업이력.md` 260731 블록.

## [Q. 28] 2026-07-31 — 판매현황 표 정렬·간격 미세조정 + 홍보 알림 당분간 중지 + 상단 메뉴 전체 전개(카테고라이징용)
> "최근 7일 추이를 가운데 정렬해주고, 장르를 좌측과의 간격을 더 벌려줘" + "프로그램하고 판매율 간격도 좀 벌려주고" + "판매율 벌릴때 누적 관객수랑도 벌려야함"
> "상단 메뉴를 세부메뉴까지 다 풀어서 써줘볼래? 좀 카테고라이징 다시하려고" / "그리고 홍보 알림 뜨는거 당분간 없애줘"

- ✅ 처리(완료): ①추이 열 가운데 정렬 + 「프로그램↔판매율(=전시 누적 관객수)」·「추이↔장르」 경계 +18px(`HP`/`pl()` · 열 폭 무접촉) ②홍보 담당자 알림 2단 중지 — 프론트 `_PROMO_NOTIFY_MUTED`(토스트·배지·목록 소거, 비파괴) + Worker `PROMO_NOTIFY_CFG.enabled=false`(신규 생성 중단, ⚠재배포 후 반영) ③상단 메뉴 전개도 = `docs/reports/20260731_상단메뉴_전개도.html`(카테고라이징 검토용). 상세 = `docs/작업이력.md` 260731b 블록.
- [추기1 · 260731c] "일단 최근 7일 추이 훨씬 우측으로 와도됨" → 열 폭 재배분 `[10,24,18,24,24]` → `[10,24,26,24,16]`(추이 칸 시작 52%→60% · 장르 84% 우측 끝) + 저장 키 `ryColW`→`ryColW2` 세대 분리. 전후 = `docs/reports/20260731_판매현황_추이우측_전후.html`.

## [Q. 29] 2026-07-31 — 상단 대메뉴 재편: 7 → 5 (한 화면 = 한 이름 = 한 자리)
> "상품 등록 > 판매 현황 첫번째로 / 연간 일정 > 대시보드 5번째로 / 회원 어쩌고 5번 메뉴로 '회원 관리' > 안에 회원 조회 / 관리 6종은 일단 톱니에 둠." + "판매현황 1번 대메뉴로 / 실적입력 3으로"
> "1 ㅇㅋ(홍보 현황 = 캘린더에만) / 2 화면으로만 통일 > 둘이 같은 이름으로 나오면 하나를 너가 더 정확한거만 남겨"

- ✅ 처리(완료): 대메뉴 **판매 현황 · 대시보드 · 캘린더 · 콘텐츠 제작 · 회원 관리(admin 전용)** 5종. 홍보 신청·온/오프라인 홍보 현황 = 캘린더 세부로 일원화(구 '홍보 신청·확인' 대메뉴 폐지) · 상품 등록·변경 = 판매 현황 세부 1번 · 구 '사업 현황' = 대시보드 ▸ **연간 실적**으로 흡수. 같은 화면을 두 이름으로 부르던 중복 청산(연간 실적·실적 조회·실적 분석·실적 입력). 딥링크 alias로 구 해시 보존. 전후 = `docs/reports/20260731_상단메뉴_재편_전후.html`. 상세 = `docs/작업이력.md` 260731d 블록.

