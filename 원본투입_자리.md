# 📥 원본 DB 투입 자리 (여기에 두면 빌더가 흡수 → 이관본 재생성)

> 원본(개인정보 포함)은 **이 레포에 커밋하지 않는다.** 이관 후에는 원본을 지운다.
> 이 파일은 "자리 표식"일 뿐 — 실제 원본 파일은 아래 위치에 두고 빌더를 돌린 뒤 삭제한다.

## 투입 위치

| 원본 종류 | 두는 곳 | 다음 명령 |
|---|---|---|
| `통합 문서1.xlsm` (SharePoint 정본) | 레포 **최상단** | `python3 tools/miso/xlsx_to_csv.py "통합 문서1.xlsm"` → `node tools/miso/build_db.mjs` → `node tools/miso/build_standalone.mjs` |
| `회원DB_*.xlsx` (고객 명부) | 레포 **최상단** | `python3 tools/miso/xlsx_to_csv.py "회원DB_*.xlsx"` → `node tools/miso/build_members.mjs` |
| 개별 시트 CSV | `data/db_export/` | `node tools/miso/build_db.mjs` |

## 규칙 (개인정보 안전)

1. **커밋 금지** — `.gitignore`가 `*.xlsm`·`*.xlsx`·`data/db_export/*.csv`·`이관본/비공개/`를 차단한다. 원본은 절대 추적되지 않는다.
2. **이관 후 삭제** — 빌더로 `이관본/`(공개 마스킹본) + `이관본/비공개/`(회원 반입 패키지)를 만든 뒤, 투입한 원본과 `data/db_export/`의 회원 CSV는 **지운다**. (이관본이 정본 산출물)
3. **공개 노출 주의** — 이 레포는 Public이다. 회원 원본(PIN·연락처·주소)은 여기서 만들어서 **MISO에만** 올리고, 공개 산출물(`이관본/data/`)은 PIN·비번·이메일·전화·내선이 마스킹된 것만 나간다.

## 현재 상태 (260730)

- 원본 `통합 문서1.xlsm`·`회원DB_주소정제_260710.xlsx` = **이관 완료 후 삭제됨**(브랜치 HEAD·디스크에서 제거).
- 산출물: `이관본/`(공개 마스킹본) + `이관본/비공개/`(회원 반입 패키지, 로컬 생성·비커밋).
- ⚠️ **main 커밋 이력(`235857d`)에는 원본이 남아 있어 라이브에서 아직 다운로드된다** — 청산 방법은 이 파일 하단 「미결」 참조.

## 미결 (운영자 결정 필요)

라이브 `promo.yeulmaru.org/통합 문서1.xlsm`·`회원DB_*.xlsx`가 **아직 200으로 다운로드된다**(main HEAD에 원본이 있어서). 닫는 방법 2단계:

1. **라이브 다운로드 차단(즉효)** = 원본을 뺀 이 브랜치를 main에 반영 → Pages가 404. (PR #460 머지 또는 원본 삭제만 담은 별도 커밋)
2. **이력 완전 제거(근본)** = `git filter-repo`/BFG로 `235857d`의 blob을 이력에서 제거 후 force-push, 또는 레포 Private 전환. **force-push는 운영자 승인 필요**(기본 금지).
